# FLUXION Voice Agent - Dependencies
# Python 3.9+ required
# Updated S85 — cleaned legacy deps (torch, spaCy, sentence-transformers removed)

# ── Core ─────────────────────────────────────────────────────────────
aiohttp>=3.9.0              # HTTP server (voice pipeline porta 3002)
python-dotenv>=1.0.0        # .env config loading

# ── STT (Speech-to-Text) ────────────────────────────────────────────
faster-whisper>=1.0.0       # CTranslate2 int8, primary STT engine
groq>=0.4.0                 # Groq API (STT cloud + LLM NLU)

# ── TTS (Text-to-Speech) — 3-tier architecture ─────────────────────
edge-tts>=7.0.0             # Quality tier: IsabellaNeural (cloud, 9/10)
piper-tts>=1.2.0            # Fast tier: Piper it_IT-paola-medium (offline, 7/10)
# SystemTTS (macOS say / Win SAPI5) — no pip dependency, OS-native

# ── NLU (Natural Language Understanding) — LLM-based ────────────────
rapidfuzz>=3.0.0            # Fuzzy matching for template fallback
jellyfish>=1.0.0            # Phonetic matching (Jaro-Winkler) for name correction
python-Levenshtein>=0.21.0  # Disambiguation handler (phonetic distance)
dateparser>=1.2.0           # Entity extraction (dates, times)

# ── VAD (Voice Activity Detection) ──────────────────────────────────
onnxruntime>=1.16.0         # Silero VAD ONNX runtime
numpy>=1.24.0               # Audio buffer manipulation

# ── Audio ────────────────────────────────────────────────────────────
sounddevice>=0.4.6          # Microphone input
soundfile>=0.12.1           # WAV file I/O

# ── Scheduler ────────────────────────────────────────────────────────
APScheduler>=3.10.0         # Reminder scheduler (-24h/-1h WA notifications)

# ── System ───────────────────────────────────────────────────────────
psutil>=5.9.0               # Hardware detection (RAM, CPU for TTS selection)

# ── Crash Reporter (S184 α.1.4) ─────────────────────────────────────
sentry-sdk[aiohttp]>=1.40.0 # No-op se SENTRY_DSN_PYTHON assente

# ── Build (dev only — not needed at runtime) ────────────────────────
# pyinstaller>=6.0.0        # Bundle voice-agent as Tauri sidecar
