# FLUXION Voice Agent - Dependencies
# Python 3.9+ required

# Core - Required
faster-whisper>=1.0.0       # STT primary: CTranslate2 int8, 4-6x faster than whisper.cpp
groq>=0.4.0
python-dotenv>=1.0.0
aiohttp>=3.9.0

# Intent Classification (Layer 1)
python-Levenshtein>=0.21.0

# STT Name Corrector — CoVe 2026 (phonetic fast-path Jaro-Winkler)
jellyfish>=1.0.0

# Entity Extraction (Day 3)
dateparser>=1.2.0

# Semantic FAQ Retrieval (Week 2)
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4
numpy>=1.24.0

# Audio (optional - for Piper TTS)
# sounddevice>=0.4.6
# soundfile>=0.12.1

# SIP/VoIP (Week 4)
pipecat-ai>=0.0.30
aiortc>=1.6.0
aioice>=0.9.0
websockets>=12.0
av>=12.0.0

# Audio processing
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0

# NLU - spaCy + UmBERTo (Layer 2-3)
spacy>=3.7.2
transformers>=4.36.0
torch>=2.1.0

# Reminder Scheduler (Gap #2 CoVe 2026 — -24h/-1h automated WA reminders)
APScheduler>=3.10.0

# System monitoring (hardware detection for FluxionTTS Adaptive)
psutil>=5.9.0

# TTS - Sara Voice (Multi-engine)
# Quality: Qwen3-TTS 0.6B CustomVoice (~1.2GB, downloaded on first run)
# transformers[cpu]>=4.40.0  — handles torch internally, CPU-only
# soundfile>=0.12.1           — WAV output from numpy arrays
# Fast fallback: Piper (always bundled, ~50ms)
piper-tts>=1.2.0
