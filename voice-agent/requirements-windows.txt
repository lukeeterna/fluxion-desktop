# FLUXION Voice Agent - Windows Dependencies
# Python 3.9+ required
#
# U4/WINPORT: Windows must run the same Sara feature stack as the canonical
# requirements.txt.  Keep platform-specific additions here only; do not fork
# the NLU/STT/TTS feature set into a second dependency graph.

-r requirements.txt

# Windows SystemTTS fallback (SAPI5)
pyttsx3>=2.90
pywin32>=306; sys_platform == "win32"
