# CI-only requirements — no heavy ML deps (torch, sentence-transformers, faiss)
# Heavy deps are lazy-imported in src/ so tests work without them

# Core
groq>=0.4.0
python-dotenv>=1.0.0
aiohttp>=3.9.0

# NLU light
python-Levenshtein>=0.21.0
dateparser>=1.2.0
numpy>=1.24.0

# Test
pytest>=7.0.0
pytest-asyncio>=0.21.0
