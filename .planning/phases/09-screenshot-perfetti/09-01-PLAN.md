---
phase: 09-screenshot-perfetti
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed-pacchetti-fedelta.sql
  - scripts/capture-pacchetti-fedelta.py
  - landing/screenshots/22-pacchetti.png
  - landing/screenshots/23-fedelta.png
autonomous: false
requirements:
  - SCRN-01
  - SCRN-02

must_haves:
  truths:
    - "Screenshot Pacchetti mostra 3 pacchetti distinti (Festa Papa, Estate, Natale) con prezzi in euro, servizi inclusi e badge sconto visivo"
    - "Screenshot Fedelta mostra almeno un cliente VIP con punteggio alto (8/10 timbri), badge VIP dorato, e progress bar quasi piena"
    - "Entrambi gli screenshot sono 1200x800 pixel, dati italiani realistici, zero overlay OS"
  artifacts:
    - path: "landing/screenshots/22-pacchetti.png"
      provides: "Screenshot della sezione Pacchetti in Impostazioni > Pacchetti fedelta"
    - path: "landing/screenshots/23-fedelta.png"
      provides: "Screenshot del tab Fedelta nel dialog cliente con tessera timbri VIP"
    - path: "scripts/seed-pacchetti-fedelta.sql"
      provides: "SQL seed con 3 pacchetti + loyalty data per 3 clienti"
  key_links:
    - from: "scripts/seed-pacchetti-fedelta.sql"
      to: "iMac SQLite DB"
      via: "sqlite3 CLI on iMac"
      pattern: "INSERT.*pacchetti"
    - from: "scripts/capture-pacchetti-fedelta.py"
      to: "landing/screenshots/"
      via: "screencapture -l on iMac"
      pattern: "screencapture"
---

<objective>
Seed pacchetti and loyalty data into the iMac database, then capture two new screenshots: Pacchetti (showing 3 seasonal packages with prices and discounts) and Fedelta (showing a VIP client with high loyalty stamp count).

Purpose: Phase 10 (Video V6) and Phase 11 (Landing) both need these screenshots to show the full product. Currently 21 screenshots exist (01-21) but Pacchetti and Fedelta are missing.
Output: 2 new PNG screenshots (22-pacchetti.png, 23-fedelta.png) + seed SQL + capture script
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/loyalty.ts
@src/components/loyalty/PacchettiAdmin.tsx
@src/components/loyalty/LoyaltyProgress.tsx
@src/components/clienti/ClienteDialog.tsx
@src-tauri/migrations/005_loyalty_pacchetti_vip.sql
@src-tauri/migrations/006_pacchetto_servizi.sql
@scripts/seed-video-demo.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and apply seed SQL for Pacchetti + Loyalty data on iMac</name>
  <files>scripts/seed-pacchetti-fedelta.sql</files>
  <read_first>
    - src-tauri/migrations/005_loyalty_pacchetti_vip.sql (table schemas: pacchetti, clienti_pacchetti, clienti loyalty fields)
    - src-tauri/migrations/006_pacchetto_servizi.sql (table schema: pacchetto_servizi)
    - scripts/seed-video-demo.sql (existing seed data — client IDs and servizio IDs to reference)
    - src/types/loyalty.ts (TypeScript types for pacchetti and loyalty)
  </read_first>
  <action>
Create `scripts/seed-pacchetti-fedelta.sql` with the following data:

**3 Pacchetti (in table `pacchetti`):**

1. "Festa del Papa" — id='pkg-festa-papa', prezzo=89.00, prezzo_originale=126.00, servizi_inclusi=3, validita_giorni=30, attivo=1, descrizione="Taglio uomo + barba + styling — regalo perfetto per la festa del papa"
2. "Estate Brillante" — id='pkg-estate', prezzo=149.00, prezzo_originale=210.00, servizi_inclusi=5, validita_giorni=90, attivo=1, descrizione="Colore + meches + trattamento + piega + taglio — pronta per l'estate"
3. "Natale Glamour" — id='pkg-natale', prezzo=119.00, prezzo_originale=165.00, servizi_inclusi=4, validita_giorni=45, attivo=1, descrizione="Piega + trattamento + colore + styling — splendi alle feste"

**Pacchetto-Servizi links (in table `pacchetto_servizi`):**

For pkg-festa-papa: link to srv-taglio-uomo (qty 1), srv-barba (qty 1), plus a third service — check existing servizi IDs in seed-video-demo.sql. Use srv-trattamento if available, otherwise pick any existing servizio.

For pkg-estate: link to srv-colore, srv-meches, srv-trattamento, srv-piega, srv-taglio-uomo (qty 1 each).

For pkg-natale: link to srv-piega, srv-trattamento, srv-colore (qty 1 each), plus srv-meches (qty 1).

**Loyalty data — UPDATE existing clients (in table `clienti`):**

- cli-anna: loyalty_visits=8, loyalty_threshold=10, is_vip=1 (VIP client, 8/10 stamps)
- cli-chiara: loyalty_visits=5, loyalty_threshold=10, is_vip=0 (halfway, 5/10 stamps)
- cli-elena: loyalty_visits=10, loyalty_threshold=10, is_vip=1 (completed full card, VIP, premio disponibile)

**Clienti-Pacchetti assignments (in table `clienti_pacchetti`):**

- cli-anna bought pkg-estate: stato='in_uso', servizi_usati=2, servizi_totali=5, data_acquisto='2026-03-01', data_scadenza='2026-05-30'
- cli-chiara bought pkg-festa-papa: stato='venduto', servizi_usati=0, servizi_totali=3, data_acquisto='2026-03-15', data_scadenza='2026-04-14'

Use `INSERT OR REPLACE` for pacchetti and `UPDATE` for clienti. Use `PRAGMA foreign_keys = OFF;` at start and `PRAGMA foreign_keys = ON;` at end (same pattern as seed-video-demo.sql).

Then apply the SQL to iMac database via SSH:
```bash
ssh imac "sqlite3 '/Users/gianlucadistasi/Library/Application Support/com.fluxion.desktop/fluxion.db' < /dev/stdin" < scripts/seed-pacchetti-fedelta.sql
```

First SCP the file to iMac, then run sqlite3:
```bash
scp scripts/seed-pacchetti-fedelta.sql imac:/tmp/
ssh imac "sqlite3 '/Users/gianlucadistasi/Library/Application Support/com.fluxion.desktop/fluxion.db' < /tmp/seed-pacchetti-fedelta.sql"
```

Verify data was inserted:
```bash
ssh imac "sqlite3 '/Users/gianlucadistasi/Library/Application Support/com.fluxion.desktop/fluxion.db' 'SELECT nome, prezzo, prezzo_originale FROM pacchetti;'"
ssh imac "sqlite3 '/Users/gianlucadistasi/Library/Application Support/com.fluxion.desktop/fluxion.db' 'SELECT id, loyalty_visits, is_vip FROM clienti WHERE loyalty_visits > 0;'"
```
  </action>
  <verify>
SSH into iMac and run:
- `sqlite3 DB_PATH 'SELECT count(*) FROM pacchetti;'` returns 3
- `sqlite3 DB_PATH 'SELECT count(*) FROM pacchetto_servizi;'` returns at least 12 (3+5+4)
- `sqlite3 DB_PATH 'SELECT nome, loyalty_visits, is_vip FROM clienti WHERE loyalty_visits > 0;'` returns 3 rows with Anna(8,1), Chiara(5,0), Elena(10,1)
- `sqlite3 DB_PATH 'SELECT count(*) FROM clienti_pacchetti;'` returns 2
  </verify>
  <acceptance_criteria>
- scripts/seed-pacchetti-fedelta.sql exists with INSERT statements for 3 pacchetti, 12+ pacchetto_servizi rows, UPDATE for 3 clienti loyalty fields, INSERT for 2 clienti_pacchetti
- iMac database contains 3 pacchetti visible in the app
- iMac database shows Anna as VIP with 8/10 loyalty visits
  </acceptance_criteria>
  <done>3 pacchetti and loyalty data are seeded in iMac database, verified via sqlite3 queries</done>
</task>

<task type="auto">
  <name>Task 2: Capture Pacchetti screenshot from iMac via CGEvent + screencapture</name>
  <files>
    scripts/capture-pacchetti-fedelta.py
    landing/screenshots/22-pacchetti.png
  </files>
  <read_first>
    - scripts/seed-video-demo.sql (for reference on existing capture patterns)
    - src/components/loyalty/PacchettiAdmin.tsx (to understand what the UI shows — card layout with pacchetti cards, WhatsApp sender, prices, badges)
    - src/pages/Impostazioni.tsx (PacchettiAdmin is under Impostazioni page, tab 'fedelta' with label "Pacchetti fedelta")
  </read_first>
  <action>
The Pacchetti UI lives in Impostazioni page, section "Pacchetti fedelta". Navigation steps on iMac:

1. Ensure Tauri dev server is running on iMac (port 1420 for Vite dev or the built app). Check with: `ssh imac "lsof -ti:1420"` or check if the .app is running.

2. IMPORTANT: Before capturing, verify the Window ID. The known WinID=297 may have changed. Run:
   ```bash
   ssh imac "python3 -c \"
   import subprocess
   result = subprocess.run(['osascript', '-e', 'tell application \\\"System Events\\\" to get {name, id} of every window of every process whose visible is true'], capture_output=True, text=True)
   print(result.stdout)
   \""
   ```
   Or use the CGEvent approach from S113 — check `/tmp/cap_tras.py` on iMac for the proven pattern.

3. Create a Python capture script `scripts/capture-pacchetti-fedelta.py` that uses CGEvent (Quartz) to:
   a. Click the "Impostazioni" sidebar item (gear icon, bottom of sidebar)
   b. Wait 1 second for page to load
   c. Scroll down to find the "Pacchetti fedelta" section OR click the sidebar sub-tab "fedelta" if the Impostazioni page has tabs
   d. Wait 1 second for the PacchettiAdmin component to render with the 3 seeded pacchetti
   e. Run `screencapture -l {WIN_ID} -o -x /tmp/22-pacchetti.png`

4. The Impostazioni page has tabs on the left sidebar. The "fedelta" tab (label "Pacchetti fedelta") is in the list. Look at the Impostazioni.tsx code — line 158 shows `{ id: 'fedelta', label: 'Pacchetti fedelta', status: status.fedelta }`. The executor MUST click on this specific tab in the Impostazioni left panel.

5. Coordinates will need to be determined dynamically. The executor should:
   - First navigate to Impostazioni (click sidebar gear icon)
   - Then identify and click the "Pacchetti fedelta" sub-tab in the Impostazioni left panel
   - Wait for PacchettiAdmin to render
   - Capture screenshot

6. SCP the screenshot back:
   ```bash
   scp imac:/tmp/22-pacchetti.png landing/screenshots/22-pacchetti.png
   ```

7. Verify dimensions:
   ```bash
   sips -g pixelWidth -g pixelHeight landing/screenshots/22-pacchetti.png
   ```
   Must be at least 1200x800.

IMPORTANT: The iMac may be in sleep. Wake it first with `ssh imac "caffeinate -u -t 5"`. Also ensure the app window is in front with `osascript -e 'tell application "Fluxion" to activate'` or similar.

COORDINATE REFERENCE from MEMORY.md:
- Window: X=360, Y=72, W=1200, H=800 (but verify each session!)
- The sidebar icons are on the left edge of the window
- CGEvent coordinates are GLOBAL screen coordinates, not window-relative

The executor MUST adapt coordinates based on the actual window position detected at runtime. Do NOT hardcode coordinates from a previous session without verifying first.
  </action>
  <verify>
- File `landing/screenshots/22-pacchetti.png` exists
- `sips -g pixelWidth -g pixelHeight landing/screenshots/22-pacchetti.png` shows >= 1200x800
- Opening the PNG visually shows 3 pacchetti cards (Festa del Papa, Estate Brillante, Natale Glamour) with prices (89, 149, 119 euro), servizi count, and discount badges
  </verify>
  <acceptance_criteria>
- landing/screenshots/22-pacchetti.png exists and is >= 1200x800 pixels
- Screenshot shows PacchettiAdmin UI with 3 distinct pacchetti cards visible
- Each card shows nome, prezzo, servizi_inclusi count, and risparmio badge
- No OS overlays, no loading spinners, no blank areas
  </acceptance_criteria>
  <done>Pacchetti screenshot captured at 1200x800+, showing 3 pacchetti with prices and discount badges</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Task 1 seeded 3 pacchetti (Festa Papa, Estate, Natale) and loyalty data (3 clients with VIP status and stamps) into iMac database. Task 2 captured the Pacchetti screenshot showing the PacchettiAdmin UI.

Next: Task 4 captures the Fedelta screenshot, but first verify the Pacchetti screenshot quality.
  </what-built>
  <how-to-verify>
1. Open `landing/screenshots/22-pacchetti.png`
2. Verify you see 3 pacchetti cards:
   - "Festa del Papa" at 89 euro (sconto da 126 euro)
   - "Estate Brillante" at 149 euro (sconto da 210 euro)
   - "Natale Glamour" at 119 euro (sconto da 165 euro)
3. Each card should show servizi inclusi count and a discount/risparmio badge
4. No OS overlays (menu bar, dock, etc.), no half-loaded UI elements
5. Italian text, euro prices, clean professional look
  </how-to-verify>
  <resume-signal>Type "approved" if screenshot looks good, or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Capture Fedelta screenshot from iMac — client dialog with VIP loyalty progress</name>
  <files>landing/screenshots/23-fedelta.png</files>
  <read_first>
    - src/components/clienti/ClienteDialog.tsx (the dialog has tabs: "dati", "fedelta", "pacchetti", "scheda" — we need the "fedelta" tab showing LoyaltyProgress)
    - src/components/loyalty/LoyaltyProgress.tsx (shows progress bar, VIP badge, timbri count, threshold, toggle)
    - src/pages/Clienti.tsx (the Clienti page with the table — clicking a client row opens ClienteDialog)
  </read_first>
  <action>
The Fedelta UI lives inside the ClienteDialog, specifically the "Fedelta" tab. To capture this:

1. Navigate to Clienti page (click the people/users icon in sidebar)
2. Wait 1 second for the client table to load
3. Click on "Anna Ferrari" row in the table (she is the VIP client with 8/10 loyalty visits)
4. Wait for ClienteDialog to open
5. Click on the "Fedelta" tab in the dialog (it's one of the tabs: Dati, Fedelta, Pacchetti, Scheda)
6. Wait 1 second for LoyaltyProgress to render — it should show:
   - VIP badge (gold crown icon)
   - Progress bar at 80% (8 out of 10 visits)
   - "8/10 timbri" text
   - VIP toggle switch (enabled)
7. Run `screencapture -l {WIN_ID} -o -x /tmp/23-fedelta.png`
8. Close the dialog (press Escape or click outside)

SCP back:
```bash
scp imac:/tmp/23-fedelta.png landing/screenshots/23-fedelta.png
```

Verify dimensions:
```bash
sips -g pixelWidth -g pixelHeight landing/screenshots/23-fedelta.png
```

The screenshot should show the full app window with the ClienteDialog open and the Fedelta tab active, displaying Anna Ferrari's loyalty progress with VIP status.

IMPORTANT: The dialog overlays the main content. The screenshot captures the entire window including the dialog overlay — this is correct and desired behavior. The dialog should be clearly visible with the LoyaltyProgress component showing the progress bar and VIP badge.

Use the same CGEvent approach and coordinate verification as Task 2. The executor should reuse the capture script or extend it.
  </action>
  <verify>
- File `landing/screenshots/23-fedelta.png` exists
- `sips -g pixelWidth -g pixelHeight landing/screenshots/23-fedelta.png` shows >= 1200x800
- Opening the PNG shows ClienteDialog with Fedelta tab active, showing Anna Ferrari's VIP loyalty progress (8/10 timbri, gold badge, progress bar at ~80%)
  </verify>
  <acceptance_criteria>
- landing/screenshots/23-fedelta.png exists and is >= 1200x800 pixels
- Screenshot shows ClienteDialog open with "Fedelta" tab active
- LoyaltyProgress component visible with VIP badge, progress bar, timbri count
- Anna Ferrari's name visible in the dialog header
- No OS overlays, no loading spinners, no blank areas
  </acceptance_criteria>
  <done>Fedelta screenshot captured at 1200x800+, showing VIP client loyalty progress with timbri and badge</done>
</task>

</tasks>

<verification>
1. `ls -la landing/screenshots/22-pacchetti.png landing/screenshots/23-fedelta.png` — both files exist
2. `sips -g pixelWidth -g pixelHeight landing/screenshots/22-pacchetti.png` — at least 1200x800
3. `sips -g pixelWidth -g pixelHeight landing/screenshots/23-fedelta.png` — at least 1200x800
4. Visual inspection: 22-pacchetti shows 3 pacchetti with Italian names, euro prices, discount badges
5. Visual inspection: 23-fedelta shows VIP client with 8/10 timbri, progress bar, gold badge
</verification>

<success_criteria>
- 22-pacchetti.png and 23-fedelta.png exist in landing/screenshots/
- Both are >= 1200x800 pixels
- Pacchetti screenshot shows 3 distinct seasonal packages with prices and discount percentages
- Fedelta screenshot shows a VIP client with high loyalty stamp count and progress visualization
- No OS overlays, loading states, or placeholder data visible in either screenshot
</success_criteria>

<output>
After completion, create `.planning/phases/09-screenshot-perfetti/09-01-SUMMARY.md`
</output>
