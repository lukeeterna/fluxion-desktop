---
phase: f02.1-nlu-hardening
plan: "03"
subsystem: voice-agent
tags: [python, fsm, nlu, regex, negation-guard, extra-entities, booking-state-machine, orchestrator]

# Dependency graph
requires:
  - phase: f02.1-01
    provides: SPOSTAMENTO hardening, no-aspetti WAIT priority, AM/PM heuristic — required so negation guard insertion does not conflict with SPOSTAMENTO guard ordering
  - phase: f02.1-02
    provides: verb-form guardrails + entity extractor — extra_entities produced by entity extractor wired here into FSM

provides:
  - "_NEGATED_CANCEL regex constant in orchestrator.py at module level"
  - "Negation guard before L1 CANCELLAZIONE handler — 'non voglio cancellare' returns CONFERMA response"
  - "extra_entities (specialty, urgency, visit_type, vehicle_plate, vehicle_brand) appended to CONFIRMING state ASKING_CLOSE_CONFIRMATION response"
  - "76 tests passing including 6 new tests for Bug 1 and Bug 5"

affects:
  - f02.1-04
  - f03-latency-optimizer

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Negation guard pattern: check _NEGATED_CANCEL.search BEFORE existing intent handler — short-circuit with override response"
    - "extra_entities suffix pattern: getattr(self.context, 'extra_entities', {}) or {} — safe for absent attribute"
    - "Vertical entity display: specialty/urgency/visit_type/vehicle_plate/vehicle_brand formatted as newline-delimited suffix"

key-files:
  created: []
  modified:
    - voice-agent/src/orchestrator.py
    - voice-agent/src/booking_state_machine.py
    - voice-agent/tests/test_booking_state_machine.py

key-decisions:
  - "_NEGATED_CANCEL guard fires only when intent_result.category == CANCELLAZIONE AND pattern matches — no false-positive risk"
  - "extra_suffix appended to both Phase 4 (pure affirmative) and Phase 6 (Groq confermato) paths — covers all CONFIRMING confirmation routes"
  - "Empty or absent extra_entities produces empty string suffix — zero behavior change for existing flows"

patterns-established:
  - "Guard-before-handler: insert intent override guard immediately before the handler it overrides, using same response/intent/layer variables"
  - "Safe extra_entities read: getattr + or {} double safety for both missing attr and None value"

# Metrics
duration: 4min
completed: 2026-03-04
---

# Phase f02.1 Plan 03: NLU Hardening Wave 2 Summary

**Negation guard (_NEGATED_CANCEL) stops "non voglio cancellare" from cancelling bookings, and extra_entities (specialty, vehicle_plate) are now shown in CONFIRMING state confirmation message**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-04T16:25:27Z
- **Completed:** 2026-03-04T16:29:42Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Bug 1 fixed: "non voglio cancellare" / "non intendo annullare" now returns "Perfetto, il suo appuntamento rimane confermato" instead of triggering cancellation flow
- Bug 5 fixed: extra_entities dict (specialty, urgency, visit_type, vehicle_plate, vehicle_brand) produced by Plan 02 entity extractor is now appended to the ASKING_CLOSE_CONFIRMATION response in both Phase 4 and Phase 6 of _handle_confirming()
- 6 new tests covering regex matching (case-insensitive), positive/negative examples, and FSM resilience to empty/absent extra_entities

## Task Commits

Each task was committed atomically:

1. **Task 1: Negation guard in orchestrator.py (Bug 1)** - `be9b90d` (fix)
2. **Task 2: Wire extra_entities into CONFIRMING state response (Bug 5)** - `da78edd` (fix)
3. **Task 3: Unit tests for Bug 1 + Bug 5** - included in `5c4753e` (test — pre-commit hook bundled with prior plan's test commit)

## Files Created/Modified
- `voice-agent/src/orchestrator.py` — Added _NEGATED_CANCEL constant (line 195) and negation guard block before L1 CANCELLAZIONE handler (line 729-737)
- `voice-agent/src/booking_state_machine.py` — Added _extra_suffix computation at start of _handle_confirming(); appended to ASKING_CLOSE_CONFIRMATION responses in Phase 4 and Phase 6
- `voice-agent/tests/test_booking_state_machine.py` — Added TestNegatedCancelGuard (2 tests) and TestExtraEntitiesInConfirming (4 tests)

## Decisions Made
- _NEGATED_CANCEL guard fires only when BOTH conditions are true: intent is CANCELLAZIONE AND pattern matches — this avoids false positives on unrelated messages
- extra_suffix uses `getattr(self.context, 'extra_entities', {}) or {}` double-safe pattern — handles missing attribute (no attr set) and None value (attr exists but is None)
- _extra_suffix appended to both ASKING_CLOSE_CONFIRMATION return paths in _handle_confirming — Phase 4 (pure affirmative) and Phase 6 (Groq confermato) — covers all confirmation routes

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
- `git commit` for Task 3 returned "nothing to commit" because the pre-commit hook (ESLint auto-fix run) had already included the test file changes in the prior plan's test commit `5c4753e`. The test classes were verified present in HEAD via `git show HEAD` and `grep`. No action needed — tests are committed and passing.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness
- Bugs 1 and 5 are fixed and tested — 76/76 PASS on test_booking_state_machine.py
- Plan 04 (remaining bugs in f02.1) can proceed — entity vertical-awareness and service synonym disambiguation
- iMac sync needed to pick up orchestrator.py and booking_state_machine.py changes: `git push origin master && ssh imac "cd '/Volumes/MacSSD - Dati/fluxion' && git pull origin master"`
- Voice pipeline restart required on iMac after sync

---
*Phase: f02.1-nlu-hardening*
*Completed: 2026-03-04*
