name: VOS Fabric G2 Complete Provisioning

on:
  push:
    branches:
      - "sol/vos-fabric-g2-worker-*"
    paths:
      - ".github/workflows/vos-fabric-g2-complete.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: vos-fabric-g2-complete
  cancel-in-progress: false

jobs:
  complete:
    runs-on: [self-hosted, macbook, fluxion]
    timeout-minutes: 30
    steps:
      - name: Complete and verify isolated vos-worker
        shell: bash
        env:
          FABRIC_SHA: 79cb3e56bfbdc3dfbe3dc04eec31d3cf2a54ff34
          CODEX_VERSION: 0.154.0
          CODEX_ARCHIVE_SHA256: d7e18b2597ae8f242f5f31ee9e90deef48dbc9edd634d9868fb6435d08c07f02
          CODEX_ARCHIVE_URL: https://github.com/openai/codex/releases/download/rust-v0.154.0/codex-x86_64-unknown-linux-musl.tar.gz
        run: |
          set -euo pipefail
          REMOTE_SCRIPT="$RUNNER_TEMP/vos-g2-complete-${GITHUB_RUN_ID}.sh"
          cat > "$REMOTE_SCRIPT" <<'REMOTE'
          #!/usr/bin/env bash
          set -euo pipefail
          export LC_ALL=C LANG=C

          fail() { printf 'G2_COMPLETE_BLOCKED=%s\n' "$*"; exit 2; }
          pass() { printf 'PASS %-32s %s\n' "$1" "${2:-}"; }

          MP=""
          for candidate in "$(command -v multipass 2>/dev/null || true)" /usr/local/bin/multipass /opt/homebrew/bin/multipass "/Library/Application Support/com.canonical.multipass/bin/multipass"; do
            if [ -n "$candidate" ] && [ -x "$candidate" ]; then MP="$candidate"; break; fi
          done
          [ -n "$MP" ] || fail MULTIPASS_MISSING
          "$MP" info vos-worker >/dev/null 2>&1 || fail VOS_WORKER_MISSING

          owner="$($MP exec vos-worker -- sh -c 'cat /var/lib/vos-fabric/worker-owner 2>/dev/null || true')"
          [ "$owner" = "VOS_FABRIC_G2" ] || fail VOS_WORKER_OWNER
          pass VOS_WORKER_OWNER "$owner"

          arch="$($MP exec vos-worker -- uname -m)"
          os_id="$($MP exec vos-worker -- sh -c '. /etc/os-release; printf "%s:%s" "$ID" "$VERSION_ID"')"
          [ "$arch" = "x86_64" ] || fail "GUEST_ARCH=$arch"
          [ "$os_id" = "ubuntu:22.04" ] || fail "GUEST_OS=$os_id"
          pass GUEST_RUNTIME "arch=$arch os=$os_id"

          "$MP" exec vos-worker -- bash -lc '
            set -euo pipefail
            sudo apt-get update
            sudo DEBIAN_FRONTEND=noninteractive apt-get install -y ca-certificates curl git jq python3 time tar
          '
          pass GUEST_DEPENDENCIES installed

          "$MP" exec vos-worker -- env \
            CODEX_VERSION="$CODEX_VERSION" \
            CODEX_ARCHIVE_SHA256="$CODEX_ARCHIVE_SHA256" \
            CODEX_ARCHIVE_URL="$CODEX_ARCHIVE_URL" \
            bash -lc '
              set -euo pipefail
              tmp="$(mktemp -d)"
              trap "rm -rf \"$tmp\"" EXIT
              curl -fsSL --retry 3 --retry-delay 2 "$CODEX_ARCHIVE_URL" -o "$tmp/codex.tar.gz"
              printf "%s  %s\n" "$CODEX_ARCHIVE_SHA256" "$tmp/codex.tar.gz" | sha256sum -c -
              mkdir "$tmp/out"
              tar -xzf "$tmp/codex.tar.gz" -C "$tmp/out"
              bin="$(find "$tmp/out" -type f -name "codex*" | head -1)"
              [ -n "$bin" ]
              sudo install -m 0755 "$bin" /usr/local/bin/codex
              /usr/local/bin/codex --version | grep -F "$CODEX_VERSION"
            '
          pass CODEX_BINARY "version=$CODEX_VERSION sha256=$CODEX_ARCHIVE_SHA256"

          gate_url="https://raw.githubusercontent.com/lukeeterna/venture-os/$FABRIC_SHA/tools/fabric/codex_worker_gate.sh"
          "$MP" exec vos-worker -- env GATE_URL="$gate_url" FABRIC_SHA="$FABRIC_SHA" bash -lc '
            set -euo pipefail
            sudo mkdir -p /usr/local/lib/vos-fabric
            curl -fsSL --retry 3 --retry-delay 2 "$GATE_URL" -o /tmp/codex_worker_gate.sh
            sudo install -m 0755 /tmp/codex_worker_gate.sh /usr/local/lib/vos-fabric/codex_worker_gate.sh
            printf "%s\n" "$FABRIC_SHA" | sudo tee /usr/local/lib/vos-fabric/FABRIC_SHA >/dev/null
            /usr/local/lib/vos-fabric/codex_worker_gate.sh preflight
          '
          pass CODEX_GATE_PREFLIGHT "fabric_sha=$FABRIC_SHA"

          auth_rc=0
          auth_out="$($MP exec vos-worker -- env VOS_CODEX_HOME=/home/ubuntu/.codex-vos-fabric bash -lc '/usr/local/lib/vos-fabric/codex_worker_gate.sh auth-status' 2>&1)" || auth_rc=$?
          printf '%s\n' "$auth_out"
          case "$auth_rc" in
            0) auth_state=GREEN ;;
            20) auth_state=REQUIRED ;;
            *) fail "CODEX_AUTH_STATUS_RC=$auth_rc" ;;
          esac

          uid="$(id -u)"
          launch_running() {
            label="$1"
            for domain in "gui/$uid" "user/$uid" system; do
              if launchctl print "$domain/$label" 2>/dev/null | grep -Eq 'state = running|pid = [0-9]+'; then return 0; fi
            done
            launchctl list 2>/dev/null | awk -v label="$label" '$3==label && $1 ~ /^[0-9]+$/ {ok=1} END{exit ok?0:1}'
          }
          launch_running com.zeroclaw.fall-detector || fail POST_GUARDIAN
          launch_running com.go2rtc || fail POST_GO2RTC
          launch_running com.mosquitto.broker || fail POST_MQTT
          curl -fsS --max-time 3 http://127.0.0.1:3002/health >/dev/null || fail POST_FLUXION_HEALTH
          lsof -nP -iTCP:3002 -sTCP:LISTEN 2>/dev/null | grep -q ':3002' || fail POST_FLUXION_3002
          if ! lsof -nP -iUDP:5080 2>/dev/null | grep -q ':5080' && ! lsof -nP -iTCP:5080 -sTCP:LISTEN 2>/dev/null | grep -q ':5080'; then fail POST_FLUXION_5080; fi
          mount | grep -q ' on /Volumes/NAS_LOCAL ' || fail POST_NAS_LOCAL
          pass PRODUCTION_REGRESSION_CHECK "Guardian/FLUXION/NAS unchanged"

          printf 'G2_PROVISIONING=GREEN\n'
          printf 'CODEX_CHATGPT_AUTH=%s\n' "$auth_state"
          printf 'G2_FULL_CERTIFICATION=PENDING_QUALIFY\n'
          REMOTE

          chmod 700 "$REMOTE_SCRIPT"
          scp -q -o BatchMode=yes -o ConnectTimeout=8 "$REMOTE_SCRIPT" imac:/tmp/vos-g2-complete.sh
          ssh -o BatchMode=yes -o ConnectTimeout=8 imac \
            "FABRIC_SHA='$FABRIC_SHA' CODEX_VERSION='$CODEX_VERSION' CODEX_ARCHIVE_SHA256='$CODEX_ARCHIVE_SHA256' CODEX_ARCHIVE_URL='$CODEX_ARCHIVE_URL' bash /tmp/vos-g2-complete.sh; rc=\$?; rm -f /tmp/vos-g2-complete.sh; exit \$rc"
