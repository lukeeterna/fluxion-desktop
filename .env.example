# FLUXION — Environment variables (NEVER commit real values)
# Copy to .env and fill with real secrets.

# Cloudflare API token (Workers/Pages/KV/D1 management)
CLOUDFLARE_API_TOKEN=

# OpenRouter — free models for asset generation (video, image, text, audio, embeddings)
# Endpoint: https://openrouter.ai/api/v1 (OpenAI-compatible)
# Free tier: 13 video / 10 image / 32 text / 2 audio / 1 embedding model — $0/M tokens
OPENROUTER_API_KEY=sk-or-v1-...

# Stripe (LIVE flip in S182 — keep TEST values until Gate 4)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Resend (transactional email — sender onboarding@resend.dev free tier)
RESEND_API_KEY=

# Worker admin endpoint Bearer (rotation tech debt S182)
ADMIN_API_SECRET=

# Lead magnet GDPR signing secret (separa da ADMIN dopo S182 ASVS finding HIGH-2)
LEAD_MAGNET_SIGNING_SECRET=
