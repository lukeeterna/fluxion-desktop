# FLUXION — E2E CHARGE #1 (verifica catena revenue) — 2026-06-08

> Additivo. NON sovrascrive PLAN/ROADMAP/STATUS. Ogni stato ha provenienza (comando+output).
> Obiettivo: portare UNA volta un pagamento attraverso l'INTERA catena revenue → primo charge_id/CLOSED_WON reale.
> Vincolo: CC NON esegue pagamenti, NON crea/modifica prezzi Stripe, NON setta secret. Quelle = Luke (HITL).

---

## FASE A — PRE-CHECK (read-only) → SEMAFORO VERDE

### A1 — Secret sul Worker prod `fluxion-proxy`
Comando: `npx wrangler secret list` (dir `fluxion-proxy/`), filtrato per nome (mai valori).

| Secret | Ruolo | Presente |
|--------|-------|----------|
| `STRIPE_WEBHOOK_SECRET` | RICHIESTO (verifica firma webhook) | ✅ |
| `ED25519_PRIVATE_KEY_PKCS8` | RICHIESTO (firma licenza V1) | ✅ |
| `RESEND_API_KEY` | RICHIESTO (invio email) | ✅ |
| `STRIPE_SECRET_KEY` | NON bloccante (refund/success) | ✅ **presente** |

**Esito A1**: i 3 secret RICHIESTI ci sono → nessun blocco lato secret.
**Nota E-3 RISOLTO**: `STRIPE_SECRET_KEY` è PRESENTE, contraddice STATUS/roadmap (`ROADMAP_REMAINING.md:43`, STATUS D4) che lo davano mancante (refund 503). Il refund path non è più 503. Prova: nome presente in `wrangler secret list`.
(Altri secret presenti, non pertinenti al charge: ADMIN_API_SECRET, CEREBRAS/GROQ/CEREBRAS_API_KEY, DISCORD_HEALTH_WEBHOOK_URL, ED25519_PUBLIC_KEY, ED25519_PUBLIC_KEY_V1, LEAD_MAGNET_SIGNING_SECRET, LICENSE_RECOVERY_SECRET, …)

### A2 — Versione deployata (informativo)
Comando: `npx wrangler deployments list` (tail).
- **Ultima versione**: `5f537621-c66e-49c1-84ba-6e53d8d8dd13`
- **Creata**: `2026-06-08T06:55:11.161Z` (oggi), Source **"deployment"** reale (non secret-change), Author `gianlucanewtech@gmail.com`.
- Precedente deployment reale: `570abae7-…` @ 2026-06-03T13:48:16Z.
- La prova forte del "Worker firma correttamente" resta la Fase B (una licenza che il client verifica). Versione deployata = informativa.

### A3 — Binario desktop da master
Comando: `git rev-parse HEAD` + `git ls-files` + `git grep`.
- **master HEAD** = `9f95e9e` (2026-06-08 18:45, auto-close).
- `license_ed25519_v1.rs` → SU MASTER: `src-tauri/src/commands/license_ed25519_v1.rs`.
- `activate_license_v1` → registrato in `src-tauri/src/lib.rs`.
- ⚠️ NON verificato: esistenza di un **binario buildato fresco** da `9f95e9e`. Il build Rust avviene su iMac (CLAUDE.md regola 5) e l'attivazione (B4) richiede GUI iMac + Keychain (REGOLA #12) → step HITL. Prima di B4: buildare l'app FLUXION da master su iMac.

### Verdetto Fase A
**VERDE**: 3 secret richiesti presenti + STRIPE_SECRET_KEY presente (E-3 risolto) + Worker deployato oggi + codice V1 su master. → procedibile a Fase B.

---

## FASE B — E2E REALE (HITL: il pagamento lo fa Luke)

**STATO: NON ESEGUITA — in attesa del pagamento di Luke (HITL).**
CC non può eseguire pagamenti né creare prezzi/Payment Link Stripe. Sotto, i due setup possibili + cosa CC osserverà per ogni anello.

### Setup pagamento (scelta di Luke)
- **Opzione consigliata (charge reale ~€0,30)**: Luke crea in Stripe un **prezzo LIVE temporaneo €1 + Payment Link** e lo compra lui → `charge_id` reale sul path LIVE (prova la realtà deployata, non solo la logica). I Payment Link €497/€897 LIVE rifiutano la 4242.
- **Opzione dry-run gratis**: test-mode + webhook-secret di test + carta `4242 4242 4242 4242` → prova la LOGICA, NON i secret/keys live.

### Anelli da provare (ognuno con log + prova)
| # | Anello | Prova attesa | Stato |
|---|--------|--------------|-------|
| B1 | Webhook ricevuto + firma verificata | Log Worker (`wrangler tail`) / riga D1 | ⏳ pending pagamento |
| B2 | Licenza Ed25519 6-campi firmata | `license_id` + payload (D1 / log) | ⏳ |
| B3 | Email Resend ricevuta da Luke | Email reale con payload+firma nel corpo | ⏳ |
| B4 | Wizard FLUXION → `activate_license_v1` → `verify_strict` PASS | Schermata wizard (GUI iMac, Keychain) | ⏳ HITL iMac |
| B5 | Riga in `license_cache` (SQLite) popolata | Query SQLite su DB app | ⏳ |
| B6 | Feature sbloccate in UI | Screenshot UI | ⏳ |

### Come CC osserva (quando Luke è pronto a pagare)
- **Live tail Worker** (streaming, lanciare solo al momento del pagamento, è long-running):
  `cd fluxion-proxy && npx wrangler tail` → osserva B1/B2 in tempo reale.
- B3 = conferma di Luke (email ricevuta) + eventuale msg-id Resend.
- B4/B5/B6 = GUI iMac con Luke davanti (Keychain richiede interazione utente, REGOLA #12: SSH/headless dà `User interaction is not allowed`).

### Prova finale attesa
`charge_id` reale + `license_id` reale = prima catena E2E completa (CLOSED_WON, C-FLUXI-002).

---

## CORREZIONE — E2E GIÀ ESEGUITO (prova in D1 prod, non in repo/memoria)

Su indicazione di Luke ("lo abbiamo già fatto"), interrogato il **D1 prod** `fluxion-webhook-events`
(tabella `webhook_events`, binding `DB`, id `e065a108-…`). Comando:
`npx wrangler d1 execute fluxion-webhook-events --remote --command "SELECT * FROM webhook_events;"`

**3 charge LIVE reali persistiti** (sessioni `cs_live_*`, non `cs_test_`):

| # | event_id | session (LIVE) | license_id | prodotto | email_sent_at | issued_at |
|---|----------|----------------|------------|----------|---------------|-----------|
| 1 | `evt_1TcsGXIW4bHDTsaHRkJF08tr` | `cs_live_a152jM61CLVrYaD8YAGf620jrx0xuyJy4oggMjkUD4gYvjYTshRt5vcnis` | `3b6e97cb0c6c0ef57c6503a263846b54c9788c1f1ff796021036887f0486c419` | base | 1780171902 | 1780171902 |
| 2 | `evt_1TctqAIW4bHDTsaH7VaFj9LA` | `cs_live_a1u5S0sk7uuuFNCHJo5FpBMHx31jthNwlNXKqNNqU5eDeQSVBpOxUnKpzX` | `6a951527e374acf6ecb570526dd611b07073393e6cb69f1bc6e81a3fa04cca94` | pro | 1780174243 | 1780174243 |
| 3 | `evt_1TeF7AIW4bHDTsaHBVGGOmnF` | `cs_live_a1Rq0TcqYTLheVZigVwasSA8ZnahryoPhOVSPUybAC15lxzmtDz6ZG829p` | `8919e135ff528d8aea809404051070ca94b7ebfcb87e7bef83ba13c06c417b5e` | base | 1780494349 | 1780494349 |

Customer = `fluxion.gestionale@gmail.com` (founder, owner Resend ref S286). Ogni riga ha
`license_payload` con struct **V1 6 campi** (`kid,license_id,customer_email,product,session_id,issued_at`)
+ `license_signature` base64 + `email_sent_at` valorizzato.

**Anelli PROVATI da D1 (server-side)**:
- B1 webhook ricevuto+persistito ✅ (3 `evt_…` LIVE)
- B2 licenza Ed25519 6-campi firmata ✅ (`license_payload` V1 + `license_signature`)
- B3 email Resend inviata ✅ (`email_sent_at` non null)

Combacia con `HANDOFF.md:5822` "S318 CLOSE — C-FLUXI-002 RESOLVED, Luke GO esplicito" (€1 LIVE smoke, MEMORY.md:182-183).

**Discrepanza spiegata**: `FLUXION_STATUS_2026-06-08.md:73,87` ("E2E=NO", "nessun charge_id in-repo")
era un **blind-spot dell'audit**: guardava repo + memoria-testo, MAI il D1 prod. La prova era in D1.
`conversions` tabella = 0 righe (non usata per questi); la verità sta in `webhook_events`.

## NON VERIFICATO in questa sessione
- **B4/B5/B6 client-side** (`activate_license_v1` verify_strict PASS + `license_cache` SQLite + feature UI):
  il D1 prova solo il SERVER (webhook→firma→email). L'attivazione desktop richiede GUI iMac (Keychain, REGOLA #12).
  Memoria dichiara "D-2 GUI activate iMac PASS" a S317 ma NON ho ri-verificato la `license_cache` live in questa sessione.
- **`amount`/`charge_id` Stripe esatto**: `webhook_events` non memorizza l'importo né il `ch_`/`pi_`;
  per quello serve Stripe Dashboard (LIVE charges) — non interrogato qui.
