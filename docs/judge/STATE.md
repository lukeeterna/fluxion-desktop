# STATE — Fonte di verità sessioni giudice

  HEAD ATTESO: f515125
  CATENA: chiusura gap audio Sara → B3-PROMOTE → PRE-PUSH AUDIT → T-GIT-REALIGN → stress-test verticali → BRAINSYNC → WINPORT → WIZARD+KBPACK → primo cliente pagante. Scadenza produzione 27/08.
  PRIMA UNITÀ NON CHIUSA: T-F3-SIP (#34v). Driver SIP di Sol integrato ed eseguito UNA volta (2026-07-30, rig sara :3003 CAPTURE=1, regstub :15062, SIP :15090, bridge :8399; [TARATURA][BOOT] reprompt_timer=22.0s, vad_speech_threshold=400 rms, E6_threshold=3). ESITI ENTRAMBI FAIL, bloccati allo STESSO gate a monte: la chiamata NON si stabilisce. Le 4 INVITE (una per tentativo, driver-porti 15170/72/74/76) ARRIVANO al go engine ma vengono rifiutate 480 Temporarily Unavailable per difetto SDP del driver — log sara3003 riga 143: level=ERROR msg="errore handler chiamata" error="more than 1 media line for type \"audio\"" (l'offerta pjsua2 del driver porta DUE m=audio). SCN-08 = FAIL: gli strike NON maturano perché nessun audio è mai stato iniettato — la strategia babble di Sol NON è stata esercitata (fallimento a monte di VAD/STT, nell'SDP dell'INVITE, non nel Silero). SCN-09 = FAIL: il reprompt_timer NON si è MAI ARMATO sulla gamba SIP perché il leg non si è mai connesso (480); [TARATURA] conferma che il timer 22.0s è configurato sul path go-engine ma senza chiamata CONFIRMED non arma. Teardown rig pulito (4 high-port FREE, zero orfani); :3002 invariato (NESSUN pid prima e dopo). Non è stato toccato il driver per far passare gli scenari. PROSSIMO: fix del DRIVER di Sol (non di Sara) — l'INVITE pjsua2 deve offrire UNA sola m=audio (rimuovere la seconda media-line audio dall'offerta SDP), poi ri-eseguire sul path SIP. Report: vos/runs/20260730/f3_sip_esiti.md + WAV f3_sip_sample.wav (campione babble iniettato; capture RTP vuota perché la chiamata non è mai stata CONFIRMED).
  DECISIONI CHIUSE — non si riaprono: M1 disclosure VERDE statico · M2 barge-in VERDE · M5 congedo VERDE provato live · M3 confirm-gated da FIX-C · congedo onesto senza «collega», con «richiamar» · TTS del rig = EdgeTTS IsabellaNeural (il «Piper ~50ms» era confabulazione) · taratura: reprompt 22.0s, vad_speech_threshold 400 rms, vad_silence ~1000ms, E6_threshold 3 · realign iMac FATTO (ff-merge a 4ce8b5e3, :3002 pid 31760 invariato) · untrack di fluxion.db* rinviato a T-GIT-REALIGN · repo pubblico per decisione founder, bonifica history obbligatoria in T-GIT-REALIGN · pricing €497 / €897 upgrade, mai canone.
  PENDENTI FOUNDER: GO su :3002 (solo a B3-PROMOTE) · sigillo estetico (nessuno aperto) · origine drift VectCutAPI (bassa priorità).
  DISCORDANZE APERTE: nessuna.
