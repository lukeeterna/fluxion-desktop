CONFIRM_FIRST
# T-VOS-FABRIC-G4-G5 — multi-worker runtime certification mandate

Founder GO source: ChatGPT instruction dated 2026-09-15 to resume VOS Fabric v2 autonomously and prove the real multi-worker G4 -> G5 chain.

Scope:
- certify G4 ZERO_PROMPT_SHUTTLING through VOS root authority, the deterministic Fabric router, and the exact certified worker registry;
- bind the exact G2 Codex and A2 Antigravity qualification references in VOS-owned routing state;
- treat Codex as breaker-OPEN only from exact real provider-capacity evidence run 34964855224 / job 104366796337; do not simulate quota or probe around it;
- execute one real PUBLIC objective through A2 DIRECT_OFFICIAL when Codex is breaker-OPEN, with A2 constrained to PUBLIC_ONLY;
- normalize the A2 result through the Fabric worker adapter, deterministically verify it, then persist a durable VOS checkpoint;
- certify G5 from a distinct process boundary by loading that durable checkpoint and resuming the A2 continuation without founder prompt shuttling;
- prove the isolated certification effect can be claimed once, same-payload replay cannot claim again, conflicting replay is rejected fail-closed, and no duplicate effect is performed;
- the certification workload is PUBLIC only; INTERNAL, CONFIDENTIAL, and SECRET data are forbidden for this gate;
- use the existing direct first-party Antigravity account authentication; no unofficial OAuth proxy substitution, API-key fallback, gateway substitution, credit fallback, credit purchase, account rotation, or quota circumvention;
- MAX_COST_USD=0 and paid fallback must remain zero;
- do not repeat the canonical A2 qualification inference merely to make a monolithic qualification job green;
- no Guardian, FLUXION, NAS, telephony, database, firewall, VPN, or production mutation;
- all certification writes are restricted to temporary files inside the isolated `vos-worker` and GitHub evidence;
- production health must be checked before and after;
- any unknown qualification, data policy, billing eligibility, router state, auth, stale reference, duplicate effect, timeout, or regression condition fails closed.

Authorization class: FOUNDER_CONFIRMED_RUNTIME_CERTIFICATION
Risk: B
Lane: WEB + isolated worker runtime
Base certified G3 SHA: 1f16ab2505480adab67b20411cf0b5819179a2b2
Fabric package must be pinned by exact SHA at execution time.
