# FLUXION — S348 resume — **R1 CHIUSO E2E VERDE. PRIMA AZIONE = R2 (distribuzione: release "Latest" v1.0.1 ha 0 asset; Windows MSI mai prodotto; no arm64). POI R3 (compliance E-3: `wrangler secret put STRIPE_SECRET_KEY`). REGOLA #29: si segue la roadmap.**

> ## >>> ESITO S347 (2026-06-08, VERDE, R1 CHIUSO end-to-end — tutto verificato live, non assunto):
> ##   (1) **D1 SBLOCCATO**: il token `FLUXION-CTO-Deploy-90d` (= `CLOUDFLARE_API_TOKEN`, id `3856673a7ec292a464fd2fda89a1db78`) ora ha scope **Account·D1·Edit** (aggiunto da Luke su dash CF). Aveva già Workers Scripts Write (deploy) + KV Write. Capability test `wrangler d1 list` → OK (vede `fluxion-webhook-events` e2e065a108 + `-test` 2fe23e8e).
> ##   (2) **MIGRATION `0003_conversions.sql` APPLICATA** su entrambi i D1 (prod `fluxion-webhook-events` + `-test`), `changed_db:true`. Tabella `conversions` + indice `idx_conversions_lead_id` verificati via `sqlite_master`.
> ##   (3) **DEPLOY**: `wrangler deploy` prod (version `5f537621`) + `wrangler deploy --env test` (version `23613867`, URL `https://fluxion-proxy-test.gianlucanewtech.workers.dev`). Entrambi col codice `recordConversion` (§6.7).
> ##   (4) **E2E SMOKE PASS** (test mode, zero costo): forgiato `checkout.session.completed` FIRMATO (HMAC whsec `STRIPE_WEBHOOK_SECRET_TEST` da `~/.claude/.env`, firma accettata = secret combacia col worker test) con `client_reference_id=lead_999`, amount 49700 → **HTTP 200, licenza emessa** (`license_id 6e3ac917…`, tier base, email_sent true) **+ conversione attribuita in D1**: query `conversions WHERE lead_id='999'` → riga `session_id=cs_test_smoke_s347_1780901938 amount=49700 email=smoke-s347@fluxion-app.com`. Script riusabile: `fluxion-proxy/scripts/smoke_conversion_s347.py` (no secrets hardcoded, legge env, UA browser per bypassare CF BIC error 1010). `agent.py won --lead N` verificato per ispezione (UPDATE leads outcome='won' + messages handoff_status='closed'); NON eseguito su lead sintetico 999 (no-op, leads.db ricreato vuoto). <<<
> ## NB GDPR/ban: R1 resta channel-neutral + handoff UMANO + risk classifier→do_not_contact. NON attivare cold-WA di massa.
> ## PRE-FLIGHT EHIWEB S348 (1 curl, NON ri-diagnosticare): `ssh imac "curl -s http://127.0.0.1:3002/api/voice/voip/status"`. S347 confermato ancora `403` → Sara Layer 2 resta BLOCKED-ON Luke→EHIWEB, NON product-core per primo €497.

> --- HANDOFF S347 ORIGINALE (FATTO, vedi esito VERDE sopra) ---
# FLUXION — S347 resume — **R1 quasi chiuso. PRIMA AZIONE = sbloccare D1 (token CF senza scope D1) → applicare migration `0003_conversions.sql` + `wrangler deploy` prod + E2E smoke. POI R2→R3 (REGOLA #29).**

> ## >>> ESITO S346 (2026-06-08, VERDE parziale): CC ha fatto da solo ciò che il vecchio header chiedeva a Luke (link Stripe). FATTO + COMMITTATO (auto-commit hook → già in HEAD, verificato `git show HEAD`):
> ##   (a) `tools/SalesAgentWA/checkout.py` — cablati i 2 Payment Link LIVE REALI dell'account Stripe (verificati via API live): **Base €497 = `https://buy.stripe.com/8x2aEYg4T8BUeLZcMi24003`** (plink_1TcpAkIW4bHDTsaH8boabwRX, price_1TD65h...), **Pro €897 = `https://buy.stripe.com/dRm4gA2e39FY47l13A24004`** (plink_1TcpAkIW4bHDTsaHfn8dioIo, price_1TD68v...). Smoke OK: genera URL con `client_reference_id=lead_<id>`. NB i Payment Link accettano `client_reference_id` via query param → flussa in `checkout.session.completed` (confermato shape Stripe).
> ##   (b) `fluxion-proxy/migrations/0003_conversions.sql` — tabella `conversions(id,lead_id,session_id UNIQUE,amount,email,at)` + indice `idx_conversions_lead_id`. `session_id` UNIQUE = INSERT OR IGNORE idempotente sui retry webhook.
> ##   (c) `fluxion-proxy/src/routes/stripe-webhook.ts` — aggiunto `recordConversion()` (best-effort, non blocca emissione licenza) + chiamata nel handler `checkout.session.completed` dopo validazione tier. **type-check exit 0**.
> ## 🔒 BLOCCO REALE (azione Luke, NON pigrizia): i token CF in `~/.claude/.env` NON hanno scope **D1** (verificato REST: `10000 auth error` su ogni endpoint D1; `CLOUDFLARE_API_TOKEN` id `3856673a7ec292a464fd2fda89a1db78` HA Workers ma NON D1; `CF_API_TOKEN` idem; nessuna sessione OAuth wrangler). Senza D1 non posso applicare la migration né (probabilmente) validare il binding al deploy.
> ## >>> PRIMA AZIONE S347 = SBLOCCO D1, scegliere UNA: **(A, più semplice)** `! cd /Volumes/MontereyT7/FLUXION/fluxion-proxy && npx wrangler login` (OAuth browser = perms pieni, bypassa il token) → poi CC procede da solo. **(B)** aggiungere `Account → D1 → Edit` al token `3856673a...` su dash.cloudflare.com/profile/api-tokens. <<<
> ## DOPO LO SBLOCCO (CC autonomo, in un colpo): (1) `cd fluxion-proxy && npx wrangler d1 execute fluxion-webhook-events --remote --file=migrations/0003_conversions.sql` (prod) + idem `fluxion-webhook-events-test`; (2) `npx wrangler deploy` (prod, NO --env test — REGOLA #28 merge≠deploy); (3) E2E smoke reale: pagamento test su un payment link con `?client_reference_id=lead_999` → verifica riga in `conversions` (D1 query) + licenza emessa → `python3 agent.py won --lead 999`. (4) chiude R1 → passa a R2 (distribuzione Windows MSI) → R3 (compliance E-3).
> ## NB GDPR/ban: R1 resta channel-neutral + handoff UMANO (no auto-invio link) + risk classifier→do_not_contact. NON attivare cold-WA di massa.
> ## PRE-FLIGHT EHIWEB S347 (1 curl, NON ri-diagnosticare): `ssh imac "curl -s http://127.0.0.1:3002/api/voice/voip/status"`. 403 → Sara Layer 2 resta BLOCKED-ON Luke→EHIWEB, NON product-core per primo €497.

> --- HANDOFF S346 ORIGINALE (superato dall'esito sopra) ---
# FLUXION — S346 resume — **R1 (Sales Agent closing layer) = CODICE FATTO. PRIMA AZIONE = (1) Luke fornisce i 2 payment link Stripe LIVE → metterli in `tools/SalesAgentWA/checkout.py`; (2) smoke E2E R1; (3) tabella D1 `conversions` + deploy worker (§6.7). Si segue la roadmap, R1→R2→R3 (REGOLA #29).**

> ## >>> ESITO S345 (2026-06-06, VERDE): decisione founder = la `FACTORY_MASTER_consolidato.md` NON è input per riscrivere la roadmap, è la SPEC DI BUILD del Sales Agent R1. Memorizzata: copia in `tools/SalesAgentWA/FACTORY_MASTER_consolidato.md` + memoria `project_factory_master_blueprint.md`. CC aveva erroneamente proposto pivot "test manuale" leggendo §5 come difetto roadmap → corretto da Luke ("cosa vendi se non hai finito il sistema?"). Roadmap INVARIATA, ripartiti da R1. <<<
> ## R1 IMPLEMENTATO (§6 del blueprint, delega general-purpose, trust-but-verify OK): creati `replies.py` (classificatore keyword hot/negative/risk/other), `handoff.py` (notifica macOS osascript + claim, NON bot ARGOS), `checkout.py` (link Stripe + client_reference_id, **PLACEHOLDER link da riempire**), `migrate_r1.sql` (applicata: 6 col + 3 indici su leads.db). Edit: `sender.py:99` `AND l.do_not_contact=0`; `agent.py` CLI `process/inbox/won` (296-389). 4 smoke test PASS (classify→`hot risk negative other`; process→0 rows clean; inbox→vuota; build_checkout_link→URL con client_reference_id). NB `leads.db` era 0-byte (svuotato): ricreato base schema via init_db, NESSUN lead reale presente (i 205 lead di aprile NON ci sono più in questo file).
> ## PRIMA AZIONE S346 (chiude R1, done-condition roadmap): (a) INPUT FOUNDER = 2 payment link Stripe LIVE (Base €497 / Pro €897) → `checkout.py` PAYMENT_LINKS + verificare nei docs Stripe che i Payment Link supportino `client_reference_id`; (b) §6.7 tabella D1 `conversions(lead_id,amount,email,at)` su worker `fluxion-proxy` + cablare handler `checkout.session.completed` + `wrangler deploy` (REGOLA #28: merge≠deploy); (c) smoke E2E reale: una conversazione di test → `agent.py process` → `inbox` mostra link → (Luke incolla link a mano, R1 è channel-neutral+HITL) → pagamento test → `client_reference_id` attribuisce → `agent.py won --lead`. POI R2 (distribuzione Windows) → R3 (compliance E-3 quick win).
> ## NB GDPR/ban: R1 è channel-neutral + handoff UMANO (no auto-invio link) + risk classifier→do_not_contact → la postura resta email-first GDPR-safe (coerente ARGOS). NON attivare invio automatico cold-WA di massa.
> ## PRE-FLIGHT EHIWEB S346 (1 curl, NON ri-diagnosticare): `ssh imac "curl -s http://127.0.0.1:3002/api/voice/voip/status"`. 403 → Sara Layer 2 resta BLOCKED-ON Luke→EHIWEB (msg pronto righe ~50-51), NON product-core per primo €497.

> --- HANDOFF S345 ORIGINALE ---
# FLUXION — S345 resume — **PRIMA AZIONE = leggere l'output di Claude AI che Luke incolla (validazione SCOPO+SEQUENZA del roadmap S344). Confrontarlo col roadmap autoritativo e decidere la sequenza definitiva, poi ESEGUIRE da `ROADMAP_REMAINING.md`. Niente freelancing (REGOLA #29).**

> ## >>> ESITO S344 (2026-06-06, VERDE): TASK #0 mandato founder S343 COMPLETO. Prodotto roadmap autoritativo UNICO = `ROADMAP_REMAINING.md` (riscritto da stato reale verificato gh/git/Glob/Grep; backup pre-rewrite `ROADMAP_REMAINING.md.bak-PRE-S344-20260606-184921`). Supersede `ROADMAP_S183_S190.md`. <<<
> ## STATO REALE ALLINEATO (in `ROADMAP_REMAINING.md`): ✅ PRONTO = payment rail prod (S331), email @fluxion-app.com (S342), custom domain HTTPS (S342), gestionale core, Sara Layer 1 testo 12 verticali (S333), license client-side implementata (gated GUI). PERCORSO REVENUE (ROI→primo €497): **R1** Sales Agent strato conversazione→checkout (`tools/SalesAgentWA/`: scraper+sender+monitor+template GIÀ fatti; MANCA link €497 + handoff, CTA punta a pages.dev) → **R2** distribuzione (BUG: release "Latest" v1.0.1 ha 0 asset; Windows .exe solo in draft v0.0.0-dev; no arm64) → **R3** compliance P0 (E-3 quick win: `wrangler secret put STRIPE_SECRET_KEY`; E-2 disclaimer testimonial landing; C-1 admin auth assente; B-2/B-3 da valutare scope).
> ## 🔒 Sara Layer 2 = BLOCKED-ON EHIWEB (`reg_status:403` confermato S344 pre-flight). NON product-core per primo €497. Azione Luke = inoltrare msg EHIWEB (righe ~40-42 sotto). NON ri-diagnosticare.
> ## >>> PRIMA AZIONE S345: Luke incolla l'output di Claude AI (validazione su `ROADMAP_REMAINING.md` + `SALES-AGENT-BLUEPRINT.md` + `config.py`, prompt dato in chat S344). LEGGERLO, confrontarlo con la SEQUENZA proposta (R1→R2→R3), e — se conferma o corregge — fissare la sequenza definitiva nel roadmap, poi partire dal primo task. I 4 nodi sub-judice S344 (autocritica): (1) il primo €497 si chiude davvero in chat WA? (2) R1 prima di R2 ha senso se mercato 80% Windows? (3) rischio ban/GDPR cold outreach WA IT; (4) R3 forse sovradimensionata (B-2/B-3 fuori-scope?). Se Claude AI segnala difetto strutturale → aggiornare roadmap PRIMA di eseguire. <<<
> ## PRE-FLIGHT EHIWEB S345 (1 curl, NON ri-diagnosticare): `ssh imac "curl -s http://127.0.0.1:3002/api/voice/voip/status"`. 403 → resta bloccato.

> --- HANDOFF S344 ORIGINALE (TASK #0 fatto, vedi esito sopra) ---
# FLUXION — S344 resume — **TASK #0 OBBLIGATORIO = ALLINEAMENTO ROADMAP. Poi si SEGUE la roadmap, sempre. Ordine founder S343: "se sgarri ancora ti licenzio".**

> ## >>> MANDATO FOUNDER S343 (2026-06-06, NON negoziabile): la roadmap è DISALLINEATA (file stale di aprile: `ROADMAP_REMAINING.md` SUPERSEDED, `ROADMAP_S183_S190.md` piano aprile mai aggiornato; il lavoro giugno non è riflesso). PRIMA AZIONE S344 = produrre UN SOLO roadmap autoritativo allineato allo stato reale verificato, poi LAVORARE SOLO da quello. VIETATO: freelancing, speculazione, "decido io un task fuori roadmap", riaprire avvitamenti (es. dominio→prodotto). Ogni task della sessione DEVE puntare a una voce della roadmap allineata. <<<
> ## STATO REALE VERIFICATO SUL TERRENO S343 (input per l'allineamento, NON ri-verificare da zero — sono prove gh/git/DB):
> ##   ✅ PRONTO: payment rail prod (S331), email deliverability @fluxion-app.com (S342), gestionale core, Sara Layer 1 testo 12 verticali (S333), license client-side IMPLEMENTATA (phone-home.ts:147-289, SaraTrialBanner.tsx) ma non behavior-verified (gated GUI iMac, REGOLA #12+#24), FAQ+runbook (commit 1aa25c9).
> ##   ❌ GAP 1 — DISTRIBUZIONE (CTO-actionable ~10h): `gh release` pubblica SOLO macOS `Fluxion_1.0.0_x64.dmg`+`.pkg`. NO Windows MSI, NO Universal Binary (no arm64), v1.0.1 release VUOTA. tauri.conf.json ha nsis configurato mai prodotto. Mercato PMI IT ~80% Windows → oggi vendi solo a Mac Intel. SECONDO blocker reale.
> ##   ❌ GAP 2 — SALES AGENT (CTO-actionable, il vero gap revenue): `tools/SalesAgentWA/` SEMI-FATTO, GIRATO LIVE 15apr (iMac `leads.db`: 205 lead, 5 inviati, 3 risposte, reply 60%). Scraper+sender Playwright anti-ban+template 6 verticali OK. MANCA strato conversazione→checkout (monitor SOLO logga, nessuna replica, nessun handoff, nessun link €497) + CTA punta a pages.dev non fluxion-app.com + LaunchAgent non caricato. Apre la conversazione ma NON la chiude.
> ##   ❌ GAP 3 — COMPLIANCE P0 non confermati chiusi (rischio AGCM): E-2 disclaimer testimonial, E-3 sk_live, B-2 WhatsApp Cloud, B-3 SDI, C-1 admin auth. git log giugno non li tocca → presumibilmente aperti, da verificare.
> ##   🔒 GATE ESTERNO — Sara Layer 2 audio = BLOCKED-ON EHIWEB (reg_status:403). Gate vendita premium REGOLA #21. NON è product-core per il PRIMO €497 (quello passa dal Sales Agent, non da Sara).
> ## SEQUENZA REVENUE suggerita per l'allineamento (priorità ROI→primo €497, tutta CTO-actionable zero-dep-esterne): (1) strato conversazione→checkout Sales Agent, (2) Windows MSI, (3) compliance landing. Sara Layer 2 resta parcheggiata BLOCKED-ON Luke→EHIWEB.
> ## PRE-FLIGHT EHIWEB (1 curl, NON ri-diagnosticare): `ssh imac "curl -s http://127.0.0.1:3002/api/voice/voip/status"`. 403 → resta bloccato (msg EHIWEB sotto).

> --- (deprioritizzato S343, NON è più la priorità: era avvitamento) ---
# FLUXION — S344 (vecchio header) — **NON riaprire la roadmap "dominio→prodotto": è AVVITAMENTO (verdetto S343 + Luke).** L'unico gate reale = Sara Layer 2, BLOCKED-ON EHIWEB (403). Azione che sblocca = SOLO Luke (messaggio EHIWEB sotto).

> ## >>> VERDETTO S343 (2026-06-06, confermato da Luke "nessuna novità, solo perdita di tempo e speculazione"): la roadmap dominio `fluxion-app.com`→prodotto è LUCIDATURA, non revenue. Ricerca S343 (verificata, vincolo #1): **item (2) license email from `@fluxion-app.com` = GIÀ FATTO** in tutti i sender (sender.ts:33, stripe-webhook.ts:190, refund.ts:185, lead-magnet.ts:274, diagnostic-report.ts:186). **item (1) swap URL proxy nel client = REJECT** (phone-home.ts:8, use-network-health.ts:8, diagnostic.rs:27, preflight.rs:29 = chiamate background invisibili; `workers.dev` è hostname nativo più robusto, custom domain aggiunge fragilità per zero beneficio). **item (3) apex→landing = brand-nicety**: apex `fluxion-app.com/`→404 JSON (bindato al worker), landing vera su `fluxion-landing.pages.dev`; ma NESSUNO vende digitando l'apex (vendita via Stripe checkout link + email già brandizzata). Spostare DNS apex su prod payment-worker = mutazione infra ad alto attrito per valore ~nullo. NON FARLO senza esplicita richiesta brand di Luke. Detail: memory `feedback_domain_roadmap_avvitamento.md`. <<<
> ## PRE-FLIGHT EHIWEB S344 (1 comando, NON avvitarsi, NON ri-diagnosticare il 403): `ssh imac "curl -s http://127.0.0.1:3002/api/voice/voip/status"`. SE `reg_status:200` → EHIWEB SBLOCCATO → Sara Layer 2 diventa l'UNICA priorità (gate vendita REGOLA #21): test chiamata reale CTO-guidata via TTS (REGOLA #23). SE ancora `403` → **NON c'è lavoro autonomo ad alto valore**: tutto il vendibile-adiacente è chiuso (payment rail S331, email S342, Layer 1 testo S333). La mossa corretta = ricordare a Luke l'azione EHIWEB e fermarsi, NON generare attività (anti-pattern S159).
> ## AZIONE LUKE (sblocca il gate reale): inoltrare a EHIWEB → "utenza SIP 0972536918 @ sip.vivavox.it registrava 200 OK il 3 giugno con credenziali invariate; ora MOR Softswitch (79.98.45.133) accetta il Digest ma risponde 403 alla REGISTER senza Warning/Reason. Verificare: (1) flag abilitazione registrazione SIP dell'account; (2) lockout/blocco anti-frode da resettare; (3) saldo/credito."
> ## NB rami license client-side (offline grace/clock-rollback/banner) restano gated GUI iMac Keychain (REGOLA #12). Pipeline Sara UP iMac bind 127.0.0.1:3002.

> --- HANDOFF S343 ORIGINALE (path ABBANDONATO: era avvitamento, vedi verdetto sopra) ---
# FLUXION — S343 resume — CABLARE il dominio nel PRODOTTO (la connessione tecnica è fatta in S342, ora va USATO). **Sara Layer 2 resta BLOCKED-ON EHIWEB (403): solo 1 curl pre-flight, NON ri-diagnosticare.**

> ## >>> ESITO S342 (2026-06-06, VERDE, tutto verificato live dal main — non assunto): custom domain `fluxion-app.com` ATTACCATO al worker prod `fluxion-proxy`. `https://fluxion-app.com/health` → **200 HTTPS** (cert live, zone active `9fa739250bcbf39d157f76752c4d18fc`, acct `22ddff3a4ef544511523a841b3dcadf8`, cert id `35aa4fb3-9f9a-4c04-8cb0-7bb7bf25ba9e`). Resend `fluxion-app.com` (id `6f986180-2eaf-41e2-8a40-53ebeefedbf0`) = **status:verified, sending:enabled**, DKIM verified. **SMOKE E2E PASS**: invio reale da `noreply@fluxion-app.com` → NON-owner `gianlucadistasi81@gmail.com` accettato (Resend msg id `4b950303-de54-4a66-bd3f-6d25ab5d5e44`) con `RESEND_TEST_KEY`. Deliverability email-cliente reale FUNZIONA. Chiusi leftover S330/S331 (go-live brandizzato) + S327 (email cliente). <<<
> ## PRIORITÀ S343 (decisione CTO REGOLA #15, da confermare con research-first REGOLA #16): il dominio è COLLEGATO ma non ancora USATO dal prodotto. Item CTO-actionable senza EHIWEB/GUI: (1) cablare `https://fluxion-app.com` come PROXY BASE URL nell'app/landing al posto di `fluxion-proxy.gianlucanewtech.workers.dev` (grep nel codice client/landing per la URL workers.dev → sostituire); (2) license email `from:` → `noreply@fluxion-app.com` dominio verificato (oggi forse manda da owner-gmail); (3) verificare se la ROOT `fluxion-app.com/` deve servire la landing marketing (oggi serve il proxy /health — la landing è separata, valutare CF Pages route). VERIFICARE ogni claim con grep/curl reale (vincolo #1), NON assumere dove sta la URL. E2E PASS obbligatorio.
> ## PRE-FLIGHT EHIWEB S343 (1 comando, NON avvitarsi): `ssh imac "curl -s http://127.0.0.1:3002/api/voice/voip/status"` (la pipeline è bind 127.0.0.1 → dal MacBook la 3002 è "closed", NON è un difetto). SE `reg_status:200` → EHIWEB sbloccato → Sara Layer 2 diventa priorità (gate vendita REGOLA #21, test chiamata reale CTO-guidata). SE ancora `403` → resta BLOCKED-ON Luke→EHIWEB (messaggio pronto sotto), lavora la roadmap dominio sopra.
> ## NB rami license client-side (offline grace/clock-rollback/banner) restano gated GUI iMac Keychain (REGOLA #12). Layer 1 testo VERDE (S333: 50 OK/3 WARN/12 verticali). Pipeline Sara UP iMac bind 127.0.0.1:3002.

> --- HANDOFF S342 ORIGINALE (FATTO, vedi esito VERDE sopra) ---
# FLUXION — S342 resume — ROADMAP RESIDUA: custom domain `fluxion-app.com` → worker prod + Resend verified. **Sara Layer 2 resta BLOCKED-ON EHIWEB (403, diagnosi chiusa S341-bis): NON ri-diagnosticare, solo 1 curl pre-flight.**

> ## >>> ESITO S341-bis (2026-06-06, voice-engineer foreground, FATTI): il sintomo "occupata" = `reg_status:403` alla REGISTER (NON NAT/INVITE). Locale 100% sano: STUN ok (mapped = IP pubblico iMac corretto), zero zombie/doppia-REGISTER, zero 486 fantasma, `.env` identico al backup, STUN-trap `voip_pjsua2.py:169` FALSIFICATA sui dati. Il 403 è del softswitch MOR EHIWEB (@79.98.45.133) DOPO aver accettato il Digest → policy/account. Fix storico "occupata" (`.planning/debug/voip-debug-diagnosis.md`, 3 bug NAT) era sul VECCHIO `voip.py` = NON applicabile (stack attuale = `voip_pjsua2.py`, reg non avviene proprio). <<<
> ## PRIORITÀ S342 = ROADMAP RESIDUA, NON Sara (Sara è BLOCKED-ON esterno). Decisione CTO S341-bis (REGOLA #15): l'unico item residuo CTO-actionable senza EHIWEB e senza GUI founder è il **custom domain `fluxion-app.com`**. Chiude DUE leftover in uno: (1) go-live brandizzato (S330/S331: NS già su CF, manca record A → attaccare al worker prod `fluxion-proxy.gianlucanewtech.workers.dev`); (2) deliverability email cliente Resend (S327: consegna a cliente reale richiede `fluxion-app.com` status:verified, oggi Resend è owner-only).
> ## PRIMA AZIONE S342:
> ##   (a) PRE-FLIGHT EHIWEB (1 comando, NON avvitarsi): `curl -s http://192.168.1.2:3002/api/voice/voip/status`. SE `reg_status:200` → EHIWEB sbloccato → Sara Layer 2 diventa priorità (gate vendita REGOLA #21): testa chiamata reale CTO-guidata. SE ancora `403` → procedi con la roadmap dominio sotto, NON ri-diagnosticare il 403 (già chiuso, è BLOCKED-ON Luke→EHIWEB).
> ##   (b) ROADMAP DOMINIO (delegare a `cloudflare-engineer`/`devops-automator`, ha Bash foreground): verificare stato DNS reale `fluxion-app.com` su CF (token in `~/.claude/.env`); attaccare il dominio al worker prod (custom domain / route su CF Workers); verificare HTTPS attivo + worker risponde su `https://fluxion-app.com/health`; poi configurare il dominio come sender verificato Resend (record DNS SPF/DKIM/DMARC) e verificare status:verified via API Resend; smoke test invio email a un indirizzo reale NON-owner. VERIFICARE ogni step con output reale (vincolo #1), E2E PASS obbligatorio (regola e2e-testing.md).
> ## BLOCKED-ON Luke (EHIWEB, indipendente da S342): messaggio pronto da inoltrare a EHIWEB (vedi sotto, blocco S341-bis). Mira al flag registrazione SIP del SINGOLO account + lockout anti-frode da resettare (a S332 registrava 200 OK con config identica → non è permanente).
> ## NB: rami license client-side (offline grace/clock-rollback/banner) restano gated su GUI iMac Keychain (REGOLA #12), NON in S342. Layer 1 testo VERDE (S333: 50 OK/3 WARN/12 verticali). Pipeline Sara UP iMac (PID 27958, bind 127.0.0.1:3002).

> ### MESSAGGIO PRONTO PER EHIWEB (inoltrare quando Luke contatta il provider):
> Oggetto: Utenza SIP 0972536918 — REGISTER rifiutata con 403 nonostante linea attiva.
> L'utenza SIP 0972536918 @ sip.vivavox.it registrava 200 OK il 3 giugno con credenziali invariate. Ora il vostro MOR Softswitch (79.98.45.133) accetta il Digest auth ma risponde 403 Forbidden alla REGISTER, senza header Warning/Reason. Verificare: (1) flag di abilitazione registrazione SIP di questo account; (2) eventuale lockout/blocco anti-frode sulla registrazione da resettare; (3) saldo/credito. Lo stato della numerazione risulta attivo, ma la REGISTER viene rifiutata post-autenticazione.

> --- HANDOFF S341-bis ORIGINALE (SUPERATO dall'esito sopra: 403 confermato, locale sano, path linphone già scartato) ---
# FLUXION — S341-bis resume — Sara Layer 2 audio. **CORREZIONE FOUNDER S341: NON cambiare carrier. EHIWEB È FUNZIONANTE, numerazione 0972536918 OK. La linea risulta "OCCUPATA" — stato GIÀ VISTO e GIÀ RISOLTO in passato (decine di sessioni). PRIMA AZIONE = verifica LIVE stato + ritrova il fix storico dell'"occupata", NON decouple su linphone.**

> ## >>> CORREZIONE FOUNDER (2026-06-04, Luke, autorità su fatto esterno EHIWEB): il path "DECOUPLE → trunk SIP free linphone" dell'handoff S340 è SCARTATO. Luke ha verificato col provider: **EHIWEB funzionante, numerazione 0972536918 corretta e attiva.** Il sintomo reale = **linea "OCCUPATA"**, condizione già occorsa in passato e già risolta (costò molte sessioni). NON è un 403-policy-da-provider come ipotizzava S339/S340, NON serve cambiare carrier. CC aveva ereditato "switch carrier" dall'handoff S340 come fatto deciso → violazione #1 (strada esterna presentata senza che il fatto — stato carrier — fosse mio da decidere). <<<
> ## PRIMA AZIONE S341-bis (NON avvitarsi, NON proporre alt-carrier): (a) `curl -s http://192.168.1.2:3002/api/voice/voip/status` + `/health` → leggere reg_status REALE adesso (S339 dava 403, ma il numero ora è confermato attivo → potrebbe essere cambiato, oppure il 403/"occupata" ha causa locale: registrazione zombie che tiene il binding, doppia REGISTER, race). (b) RITROVARE IL FIX STORICO: `grep -ri "occupat\|busy\|486\|già registrat\|doppia registr\|stale\|zombie\|binding" .claude/ wiki/ *.md` + cercare nelle memorie agent voice-engineer come fu risolto l'"occupata" la volta scorsa. (c) Solo DOPO aver letto stato live + fix storico → diagnosi mirata, MAI ri-proporre linphone/altro trunk. Delegare diagnosi a voice-engineer (foreground SSH).
> ## NB DE-RISKING S340 ancora valido come informazione (NON come piano linphone): trappola STUN hardcoded `stun.voip.vivavox.it:3478` a `voip_pjsua2.py:~169` senza env override → STUN-timeout che può disturbare la registrazione EHIWEB stessa; backup `.env` iMac = `voice-agent/.env.bak-PRE-S340-20260604-172511` (mode 600). La config EHIWEB legittima resta in `voice-agent/.env` (VOIP_SIP_* → sip.vivavox.it), NON toccarla salvo diagnosi.
> ## CHIUSURA S341 anomala: CC hard-bloccato dal gate context a 80% (RAW, probabilmente gonfiato — vedi REGOLA #27) PRIMA di poter eseguire il curl di verifica live. Nessuna azione tecnica fatta in S341, solo questa correzione di rotta. Layer 1 testo resta VERDE (S333: 50 OK/3 WARN su 12 verticali).

> --- HANDOFF S341 ORIGINALE (SCARTATO da correzione founder sopra: il path linphone NON si esegue) ---
# FLUXION — S341 resume — Sara Layer 2 audio via trunk SIP free. **Path DECOUPLE verificato (sip.linphone.org). BLOCKED-ON Luke: creare 2 account SIP free. PRIMA AZIONE = al GO credenziali, switch trunk + test E2E in un colpo.**

> ## >>> ESITO S340 (2026-06-04, voice-engineer foreground): DECOUPLE da EHIWEB confermato come strategia. **STEP 1 trunk free SCELTO e VERIFICATO su dati reali = `sip.linphone.org`** (Flexisip free Belledonne, 796k utenti, registrar RFC 3261 standard → registrabile da pjsua2, audio inbound/outbound tra utenti del dominio). DNS SRV reali `_sip._udp.sip.linphone.org` → `sip11/sip12.linphone.org:5060`. Scartati loopback (morto strutturale) ed EHIWEB (403 provider). <<<
> ## >>> BLOCKED-ON Luke (azione umana esterna, NON avvitamento): creare **2 account SIP free su `subscribe.linphone.org`** (uno per Sara, uno per harness/chiamante). Il portale è SPA con verifica SMS/email + form interattivo → NON automatizzabile da SSH. Servono **username+password dei 2 account**. <<<
> ## DE-RISKING FATTO S340 (così al GO il test parte in un colpo): (1) backup `.env` creato+verificato su iMac = `voice-agent/.env.bak-PRE-S340-20260604-172511` (496B, mode 600) — NON committare, è artefatto. (2) Config trunk si imposta SOLO via env `VOIP_SIP_SERVER/USER/PASS`, nessun hardcode. (3) **TRAPPOLA identificata**: `stun_server` default `stun.voip.vivavox.it:3478` è HARDCODED a `voip_pjsua2.py:~169` SENZA env override → log mostravano STUN-timeout verso vivavox che bloccavano la registrazione. Allo switch va NEUTRALIZZATO (puntare a STUN linphone o vuoto, eventualmente aggiungere `VOIP_STUN_SERVER` env). Memoria agent completa: `.claude/agent-memory/voice-engineer/project_layer2_sip_free_trunk_decouple.md`.
> ## PRIMA AZIONE S341 (al GO con credenziali da Luke): delegare a voice-engineer → (a) edit `.env` (VOIP_SIP_SERVER=sip.linphone.org + user/pass + FIX STUN trappola); (b) restart pipeline iMac; (c) verifica `curl http://192.168.1.2:3002/api/voice/voip/status` → `registered:true, reg_status:200`; (d) harness `voice-agent/scripts/sara_audio_harness.py` (NON modificarlo) dal 2° account chiama Sara, WAV "Buongiorno, vorrei prenotare un appuntamento" (PCM16 8kHz mono) → cattura risposta RTP → trascrivi → verifica pertinenza. Chiude TODO[SIP-LIVE] + gate Sara Layer 2 (REGOLA #21).
> ## NB EHIWEB 0972536918 resta `403` BLOCKED-ON Luke (domanda al provider, vedi sotto) — serve SOLO per chiamate clienti veri in prod, NON per validare Layer 2. Layer 1 testo già VERDE (S333: 50 OK/3 WARN su 12 verticali).

> --- HANDOFF S340 ORIGINALE (premessa, ora con esito sopra) ---
# FLUXION — S340 resume — Sara Layer 2 audio. **EHIWEB 403 NON risolto dalla riattivazione (empirico). DECOUPLE: testare Layer 2 con trunk SIP free, NON aspettare EHIWEB.**

> ## >>> ESITO S339 (2026-06-04, fatti, zero speculazione): la riattivazione del numero da parte di EHIWEB NON ha sbloccato la registrazione. Pipeline Sara riavviata (PID ~67091, health 200), ma `/api/voice/voip/status` → `registered:false, reg_status:403`. Traccia SIP raw catturata (log pjsip lvl 5): REGISTER→401 challenge→REGISTER+Digest→**403 Forbidden NUDO** dal `Server: MOR Softswitch` (EHIWEB, Asterisk dietro). Digest ACCETTATO (realm `asterisk` combacia, URI/From/To/Contact tutti corretti) → **NON è credenziali, NON è realm, NON è IP**. 403 = rifiuto policy/account POST-auth, senza `Warning`/`Reason`/body → motivo NON esposto nel protocollo. Timeline a credenziali invariate: S332 `200` → S333 `408` → S334-S339 `403` = cambiamento LATO ACCOUNT EHIWEB. <<<
> ## IPOTESI IP-WHITELIST DEFINITIVAMENTE FALSIFICATA (founder-input + dati): (1) Digest accettato e 403 arriva DOPO l'auth → autorizzazione non legata all'IP; (2) provider con utenze residenziali a IP dinamico non può fare ACL per IP. NON ri-proporla. (NB IP iMac comunque cambiato S334→S339: 151.72.9.90 → 151.72.30.102, ma è irrilevante.)
> ## BLOCKED-ON Luke (azione esterna, una domanda precisa a EHIWEB): "interno 0972536918 registrava 200 OK giorni fa con le stesse credenziali, ora il vostro MOR Softswitch risponde 403 al REGISTER DOPO aver accettato il Digest auth — verificate stato account, credito/saldo e flag abilitazione registrazione." Verifica sblocco: `curl http://192.168.1.2:3002/api/voice/voip/status` → `registered:true, reg_status:200`.
> ## PRIMA AZIONE S340 = DECOUPLE da EHIWEB (NON aspettare il provider per testare Layer 2). Il test Layer 2 (Sara risponde a chiamata vocale reale) NON richiede il numero EHIWEB: serve QUALSIASI trunk SIP che passi per la rete (il timing rete diluisce la race che fa crashare il loopback 127.0.0.1). Path raccomandato €0: registrare Sara su un account SIP free (es. `sip.linphone.org`) + secondo account per chiamarla via harness/softphone → audio E2E reale senza EHIWEB. EHIWEB resta necessario SOLO per chiamate clienti veri in prod. Delegare a voice-engineer (foreground SSH/Bash). VERIFICARE prima la fattibilità del trunk free scelto (vincolo #1) e che la registrazione Sara verso quel trunk vada a 200 OK.
> ## NB loopback 127.0.0.1 PARCHEGGIATO (bug strutturale pjsua2 confermato 3 cicli S336-S338, version+config independent). Asterisk ARI archiviato. Layer 1 testo già VERDE (S333: 50 OK/3 WARN su 12 verticali).

> --- HANDOFF S339 ORIGINALE (premessa sbloccata poi FALSIFICATA empiricamente, vedi esito S339 sopra) ---
# FLUXION — S339 resume — Sara Layer 2 audio. **EHIWEB 403 RISOLTO (numero riattivato). Gate loopback PARCHEGGIATO (S338 verdetto falsificato). PRIMA AZIONE = TEST CHIAMATA REALE SARA via provider.**

> ## >>> SBLOCCO EHIWEB (2026-06-04, founder-input): il `reg_status:403` persistente (S332-S337) era causato dal **numero 0972536918 temporaneamente DISATTIVATO dal provider EHIWEB/vivavox**. Luke conferma: **ora RIPRISTINATO e FUNZIONANTE.** Il BLOCKED-ON esterno è chiuso. Niente whitelist IP / credito / password da toccare — era disattivazione lato provider. <<<
> ## PRIMA AZIONE S339 = OPZIONE 1 (path provider reale, raccomandata S338 e ora sbloccata): (1) Pipeline Sara è DOWN (porta 3002) — riavviarla su iMac PRIMA di tutto. (2) Verificare registrazione SIP verde: `curl http://192.168.1.2:3002/api/voice/voip/status` → atteso `registered:true, reg_status:200` (era 403). (3) Se verde → TEST CHIAMATA REALE Sara CTO-guidato (REGOLA #23: CTO parla via TTS su iMac, NON chiedere a Luke di chiamare). Le chiamate provider S244 NON crashavano (timing rete diluisce la race che il loopback innescava) → questo è il canale Layer 2 funzionante. (4) Validare Sara sui verticali via audio reale (REGOLA #21 "soddisfa pienamente il cliente") — scenari STT-sensitivi via voce, il resto già VERDE Layer 1 testo (S333: 50 OK/3 WARN su 12 verticali).
> ## NB loopback 127.0.0.1: resta non-testabile (bug strutturale pjsua2 confermato S338, vedi sotto) ma NON è più un blocco — il provider reale è la via di validazione. Asterisk ARI archiviato (non serve più).

> ## >>> S338 ESITO BINARIO (2026-06-04, voice-engineer foreground): config del giudice GIÀ implementata nel codice = no-op. Verificato live su iMac: `threadCnt=0` (riga 879) + `mainThreadOnly=True` (riga 880) + libCreate(824)/libHandleEvents(806) STESSO `_pjsua2_thread` + `libInit(ep_cfg)` (902) dopo i flag (ordine corretto) + pjsua2 `2.15.1` live. Restart pipeline + harness loopback `sip:0972536918@127.0.0.1:5080` → **Sara CRASHA IDENTICO**. Log probante fresco (`/tmp/sara-pjsip-s244.log`): `conference.c onCallMediaS Add port 2 (sara_bridge) queued` → `Fatal Python error: Aborted` in `libHandleEvents` (voip_pjsua2.py:806). **`mainThreadOnly=True` è attivo MA NON marshalizza i callback media C-side** (`onCallMediaState`/resample/conferenza) — controlla solo i callback SIP/INV. L'intuizione del giudice (marshaling integrata) era giusta come intent, SBAGLIATA sull'implementazione reale di pjsua2: quel flag non tocca i media callback. <<<
> ## CRITERIO STOP BINARIO DEL GIUDICE SODDISFATTO → diagnosi config FALSIFICATA. Per istruzione esplicita del verdetto (riga 54 di `.claude/cache/agents/s337/claude-ai-response.md`): **NO 4° ciclo autonomo, NO Asterisk senza NUOVO verdetto esterno.** Gate Sara Layer 2 SPOSTATO su "validazione con chiamata reale post-403". Loopback PARCHEGGIATO (resta utile come rete di regressione SOLO se un giorno funzionasse, ma non è prerequisito).
> ## ROOT CAUSE FINALE (3 cicli, firma identica): bug STRUTTURALE pjsua2 — `onCallMediaState` C-side gira su media-thread non disabilitabile né marshalabile; port-add conference da lì setta owner group-lock, `_pjsua2_thread` fa l'unset in `libHandleEvents` → owner mismatch → SIGABRT. Version-independent (2.15.1 e 2.16-dev identici), config-independent (threadCnt/mainThreadOnly/affinity tutti corretti). NON Python-fixabile, NON config-fixabile.
> ## PRIMA AZIONE S339 = DECISIONE FOUNDER (NO azione tecnica autonoma su questo gate, vincolo #1c). Tre opzioni residue, decisione di Luke:
> ##   (1) **Path provider reale EHIWEB** come UNICO canale Layer 2: sbloccare il `reg_status:403` (BLOCKED-ON Luke, pannello EHIWEB — vedi sotto), poi testare Sara su chiamata reale CTO-guidata. Le chiamate provider S244 NON crashavano (timing diluito evita la race loopback). €0, nessun nuovo daemon. RACCOMANDATA.
> ##   (2) **Asterisk ARI** come media layer, Sara solo cervello (rimuove conference bridge pjsua2). Richiede NUOVO verdetto giudice prima di partire (rischio build Big Sur no-AVX2, 2-3gg realistici). Parcheggiata F2/F3.
> ##   (3) Accettare loopback non-testabile, gating Layer 2 esclusivamente su (1).
> ## Se Luke sceglie (2) Asterisk → scrivere prima prompt per giudice esterno (vincolo #1c, evidenza 3 cicli falliti). Memoria agent completa: `.claude/agent-memory/voice-engineer/project_sip_loopback_crash_pjsip216.md` (sezione "S338 binary run").
> ## STATO iMac: pjsua2 2.15.1 in `lib/pjsua2/`, backup 2.16-dev in `lib/pjsua2.backup-PRE-S337-20260604-125255/`. Pipeline Sara DOWN dopo crash harness — riavviare a inizio S339 se serve. Branch iMac `fix/license-interop-r01-s327`.

> --- HANDOFF S338 ORIGINALE (SUPERATO da esito sopra) ---
# FLUXION — S338 resume — Sara Layer 2 audio. **S337 DOWNGRADE 2.15.1 FATTO ma NON RISOLVE. Ipotesi versione FALSIFICATA.**

> ## >>> VERDETTO GIUDICE RICEVUTO (`.claude/cache/agents/s337/claude-ai-response.md`): OPZIONE 1 fix threading — ma NON il deferral già falsificato. Fix = marshaling INTEGRATA pjsua2: `uaConfig.threadCnt=0` + `uaConfig.mainThreadOnly=true` + `libCreate()`/`libHandleEvents()` sullo STESSO thread. NON Asterisk. NON abbandonare il loopback. Root cause = pjsua2 issue #1519 (media/sound thread non disabilitabile chiama callback → owner group-lock mismatch). `mainThreadOnly` marshalizza ANCHE i callback interni pjsip → risolve il crash su `Add port 1` che il deferral Python non poteva toccare. <<<
> ## PRIMA AZIONE S338 (ciclo BOUNDED hard-cap 2h, autorizzato #1c con esito binario): (a) ispeziona config attuale in `voice-agent/src/voip_pjsua2.py`, registra i 3 valori (threadCnt, mainThreadOnly, dove gira libCreate vs libHandleEvents) — la sola lettura dirà quale dei 3 è violato; (b) applica i 3 punti; (c) restart pipeline + ri-run harness loopback. **STOP NETTO**: se ancora aborta su `grp_lock_unset_owner_thread` → diagnosi config FALSIFICATA → sposta gate su "chiamata reale post-403", parcheggia loopback, NO 4° ciclo né Asterisk senza nuovo verdetto esterno. VERIFICARE le fonti del giudice (doc PJSIP threadCnt, issue #1519, UaConfig.mainThreadOnly) PRIMA di applicare (vincolo #1).

> ## >>> S337 ESITO (2026-06-04, voice-engineer): pjsip downgrade 2.16-dev → 2.15.1 ESEGUITO e VERIFICATO (`libVersion().full == 2.15.1`, build da sorgente iMac, SWIG 4.4.1, static x86_64 Big Sur, installato in `lib/pjsua2/`). **MA Sara CRASHA ANCORA identico sull'INVITE loopback** (SARA_DEAD, mai 200 OK, harness solo `100 Trying`→timeout). **2.15.1 HA LA STESSA op-queue asincrona di 2.16-dev** — log Sara mostra ancora `conference.c onCallMediaS "Add port N queued"` + `possibly re-registering existing thread`. **L'ipotesi "2.15.1 è sincrona" era SBAGLIATA.** <<<
> ## ROOT CAUSE REALE (rivista): STRUTTURALE, non version-specific. Sara fa port-add conference da thread `onCallMediaState`; group-lock unset su `_pjsua2_thread` in `libHandleEvents` → mismatch owner → abort. Il downgrade non tocca questo.
> ## NEXT S338 (decisione architetturale founder/main, NO 3° fix Python — REGOLA #1c): opzione (1) forzare port-add nel contesto del thread pjsua worker (`libRegisterThread`/eseguire add dentro l'event thread così l'owner del group-lock combacia); opzione (2) ESCALATION Asterisk ARI (N5 dossier) come layer SIP/media, Sara solo cervello — elimina la conference bridge pjsua2. Dettaglio completo: `.claude/agent-memory/voice-engineer/project_sip_loopback_crash_pjsip216.md` (sezione S337 UPDATE).
> ## Stato lib iMac: pjsua2 2.15.1 installato in `lib/pjsua2/`; backup 2.16-dev = `lib/pjsua2.backup-PRE-S337-20260604-125255/` (22MB, restore point). Nessun motivo di revertire (nessuna delle 2 versioni funziona per loopback). Sara pipeline DOWN dopo l'ultimo crash harness — riavviarla all'inizio S338.

> --- HANDOFF S337 ORIGINALE (parzialmente superato) ---
# FLUXION — S337 resume — Sara Layer 2 audio. **ROOT CAUSE TROVATA: pjsip 2.16-dev. NEXT = downgrade pjsip 2.15.1.**

> Scritto 2026-06-03 a chiusura S336. **FINDING DECISIVO**: l'INVITE SIP diretto P2P all'iMac FUNZIONA (gate sbloccato lato routing), ma la pipeline Sara **crasha** processando l'INVITE per un bug di **pjsip 2.16-dev** (op-queue / group-lock owner-thread sul commit della conference port). Il crash NON è chiudibile lato Python. **Unica via: downgrade pjsip → 2.15.1** (conference sincrona, niente op-queue). Gate Sara Layer 2 NON raggiunto = handoff strutturato, non verde.

> ## >>> PRIMA AZIONE S337: downgrade pjsip 2.16-dev → 2.15.1 sull'iMac, poi ri-run harness. <<<
> Delegare a voice-engineer (REGOLA #25, foreground per SSH/Bash). Runbook = "S244 path B1". Dopo il downgrade: ri-lanciare `voice-agent/scripts/sara_audio_harness.py` con INVITE diretto e validare il flusso audio E2E. Memory dettagliata dell'agent: `.claude/agent-memory/voice-engineer/project_sip_loopback_crash_pjsip216.md`.

## STATO — cosa è SOLIDO (non rifare)
- ✅ **INVITE diretto FUNZIONA** (chiude TODO[SIP-LIVE] ~36/~345). URI vincente: `sip:0972536918@127.0.0.1:5080` (harness su `127.0.0.1:5070`). Sara risponde `100 Trying` + `Answering call 0: code=200`, codec speex negoziato, bridge `sara_bridge` creato. La registrazione EHIWEB 403 è IRRILEVANTE per il test loopback (confermato).
- ✅ Harness `voice-agent/scripts/sara_audio_harness.py` (committato 196b491) = CORRETTO, non modificarlo. WAV TX gen OK (PCM16 8kHz mono, ricetta `say -o raw.aiff` + `afconvert -f WAVE -d LEI16@8000 -c 1`).
- ✅ Layer 1 testo VERDE (S333: 50 OK / 3 WARN / 0 FAIL su 12 verticali) — path HTTP `/api/voice/process`, NON tocca SIP, NON impattato dal bug.

## ROOT CAUSE (pjsip 2.16-dev) — diagnosi S336 completa
- Crash via `faulthandler`: `Fatal Python error: Aborted (SIGABRT)` nel thread `_pjsua2_thread` dentro `libHandleEvents` (`voip_pjsua2.py:806`), preceduto da pjsip C-log `conference.c onCallMediaS "Add port N queued"` + `os_core_unix.c "possibly re-registering existing thread"`. È l'assertion `grp_lock_unset_owner_thread` (lock.c): owner del group-lock settato dal thread di callback `onCallMediaState`, unset da `_pjsua2_thread`.
- pjsip attivo = **2.16-dev** (`libVersion` verificato). L'INVITE loopback dispatcha `onCallMediaState` su un media-thread dedicato → espone la race che le chiamate provider (S244) evitavano per timing.
- **Fix S243 esistente** (deferiva `startTransmit` nel drainer `drain_pending_bridges`) = INSUFFICIENTE: lasciava `createPort` dentro `onCallMediaState`.
- **Fix S335 tentato** (deferiva ANCHE la port creation `ensure_port`+`getAudioMedia` nel drainer): ha solo SPOSTATO il crash da `Add port 2 (sara_bridge)` → `Add port 1` (conference-port della call-leg, creata INTERNAMENTE da pjsip durante `answer()`, fuori dal controllo Python). **Conclusione: il bug op-queue 2.16-dev colpisce porte che non gestiamo → NON chiudibile lato Python.**

## S337 — PIANO (CTO, REGOLA #15)
1. **Downgrade pjsip 2.16-dev → 2.15.1** (iMac, runbook S244 path B1). NB: 2.15.1 ha conference API SINCRONA (`Conf add port N` immediato, niente op-queue) → niente owner-thread mismatch. Verificare che il path chiamate provider (S244) resti funzionante. Delegare a voice-engineer.
   - NON usare il backup `lib/pjsua2.backup-2.16dev-20260515` (è ancora 2.16-dev, stesso bug). Serve build/lib 2.15.1 reale.
2. **Ri-run harness** con INVITE diretto `sip:0972536918@127.0.0.1:5080`, WAV "Buongiorno, vorrei prenotare un appuntamento" → Sara NON crasha → 200 OK trasmesso → bridge attivo → cattura risposta RTP di Sara su WAV (durata >0) → trascrivi (whisper.cpp/Groq) → verifica Sara ha CAPITO+risposto pertinente. Chiude TODO[SIP-LIVE] ~273 (bridge wiring) e ~374 (cattura RTP).
3. **Estendere a golden-path per verticale via audio** (REGOLA #21: "soddisfa pienamente il cliente"). Scenari STT-sensitivi via audio; il resto già VERDE Layer 1 testo.

## STATO iMac (per ripartenza pulita)
- Branch iMac = `fix/license-interop-r01-s327` (preesistente da R-01, NON master). Tree pulito.
- **Fix S335 STASHATO** (recuperabile): `git stash list` → `stash@{0}` "s335-sip-loopback-fix-pjsip216-deferred-port". Backup pre-fix: `/tmp/voip_pjsua2.py.bak-s335-1780518361` (volatile, /tmp). **Probabilmente reso SUPERFLUO dal downgrade 2.15.1** (niente op-queue → niente deferral necessario). Recuperare solo se 2.15.1 mostrasse ancora una race.
- Pipeline Sara: UP+healthy (era PID 3410; il processo in memoria aveva il fix S335 caricato, il disco è ora a HEAD post-stash — il downgrade comporterà comunque restart, stato si normalizza).

## BLOCKED-ON (Luke/esterno, NON blocca gate Sara Layer 2)
- EHIWEB reset binding SIP (`reg_status:403` persistente): serve SOLO per chiamate clienti reali in prod, NON per il test CTO-guidato loopback. Ri-escalation operatore: reset garantito non ha funzionato; verificare reset propagato + IP whitelist (IP pubblico iMac `151.72.9.90`) + account/credito; se password rigenerata → aggiornare `voice-agent/.env` VOIP_SIP_PASS.
- Custom domain `fluxion-app.com`: NS su CF, no record A → attaccare a worker prod per go-live brandizzato.
- Rami client-side license tsc-only (offline grace/clock-rollback/banner): GUI iMac Keychain (REGOLA #12).
